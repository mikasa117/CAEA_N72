/* -----------------------------------------------------------------------------
  Filename:    appdesc.c
  Description: Implementation example for the proper usage with CANdesc.
                
                Manufacturer: Vector
                EcuDocFile:   C:\Users\leisz70544\Desktop\K1LF01C_CFG_Canbedded\src\K1LF01C-E-CDD-A1.cdd
                Variant:      CommonDiagnostics

  Generated by CANdelaGen, Thu Sep 05 19:55:56 2019
 
 ----------------------------------------------------------------------------- */
/* -----------------------------------------------------------------------------
  C O P Y R I G H T
 -------------------------------------------------------------------------------
  Copyright (c) 2001-2013 by Vector Informatik GmbH. All rights reserved.
 
  This software is copyright protected and proprietary to Vector Informatik 
  GmbH.
  
  Vector Informatik GmbH grants to you only those rights as set out in the 
  license conditions.
  
  All other rights remain with Vector Informatik GmbH.
 -------------------------------------------------------------------------------
 ----------------------------------------------------------------------------- */

/* -----------------------------------------------------------------------------
    &&&~ History
 ----------------------------------------------------------------------------- */

/* ----------------------------------------------------------------------------
  Please note, that the demo and example programs only show special aspects of 
  the software. With regard to the fact that these programs are meant for 
  demonstration purposes only, Vector Informatik's liability shall be expressly 
  excluded in cases of ordinary negligence, to the extent admissible by law or 
  statute.
  ---------------------------------------------------------------------------- */

/* 
------------------------------------------------------------------------------------------------------------------------
                                             A U T H O R   I D E N T I T Y                                              
------------------------------------------------------------------------------------------------------------------------
Initials   Name                  Company                                                                                
---------- --------------------- ---------------------------------------------------------------------------------------
Ade        Alexander Ditte       Vector Informatik GmbH                                                                 
Aey        Amr Elazhary          Vector Informatik GmbH                                                                 
Ktw        Katrin Thurow         Vector Informatik GmbH                                                                 
Mhe        Matthias Heil         Vector Informatik GmbH                                                                 
Rpk        Patrick Rieder        Vector Informatik GmbH                                                                 
Sa         Mishel Shishmanyan    Vector Informatik GmbH                                                                 
Wse        Wilfried Steudle      Vector Informatik GmbH                                                                 
------------------------------------------------------------------------------------------------------------------------
                                            R E V I S I O N   H I S T O R Y                                             
------------------------------------------------------------------------------------------------------------------------
Date       Version  Author Modification type Issue Id      Affects                        Description                    
---------- -------- ------ ----------------- ------------- ------------------------------ -------------------------------
2009-09-10  6.00.00
                    Sa     Modified          ESCAN00031159 Vector(UDS)                    Possible wrong cycle timings for RoE event monitoring
                    Ktw    Fixed             ESCAN00031152 Vector(UDS)                    Positive response for RoE 01 and 03 is truncated
                    Sa     Modified          ESCAN00034761 Vector(UDS)                    The security level specific seed check has no effect
                    Ade    Modified          ESCAN00034789 Vector(UDS)                    Change UACC implementation from semaphore protected access to context specific access
                    Sa     Modified          ESCAN00034880 UDS                            Disable call of DescStateTask() in DescTask() if HIGH_PERFOMANCE_MODE is active
                    Sa     Added             ESCAN00035198 UDS                            New API DescIsSuppressPosResBitSet
                    Sa     Fixed             ESCAN00028409 All                            No response on request         
                    Sa     Modified          ESCAN00029770 All                            Compiler warning for enumeration item with value 0xFF on enum-type = char
                    Sa     Added             ESCAN00030116 All                            Provide IsoTp-Callback prototypes in desc.h/danisisotp.h
                    Sa     Added             ESCAN00030087 All                            Support of internal diagnostic requests over IsoTP
                    Sa     Fixed             ESCAN00030981 All                            Switch to suppress multitiple addressing check
                    Sa     Fixed             ESCAN00029915 All                            CANdesc files can not be written
                    Sa     Modified          ESCAN00031158 All                            Ring-buffer feature enabled for non-ISO-TP DANIS drivers
                    Sa     Added             ESCAN00031156 All                            Added DANIS abstraction for testerId and comChannel
                    Ktw    Fixed             ESCAN00031533 All                            Compiler Warning: Unused variable tpTxChannel
                    Sa     Fixed             ESCAN00027182 All                            Compiler Warning: variable "svcInstFailedBytePosMask" is possibly uninitialized
                    Sa     Fixed             ESCAN00034934 All                            Wrong null-terminated and leading size byte string data object total length calculation
                    Ktw    Fixed             ESCAN00035554 All                            Null pointer access in DanisBoschTp
                    Sa     Modified          ESCAN00036996 All                            Switching between ANSI C and hardware optimized bit-type
                    Ktw    Added             ESCAN00037874 All                            Support multi identity mode - VSG
2009-01-11  6.01.00
                    Ktw    Modified          ESCAN00040135 All                            CanChannelIdentityAssignment is generated in RAM section
2010-03-18  6.02.00
                    Ktw    Added             ESCAN00042440 Vector(UDS)                    Support ASR 3.0 interface for 0x19 0x01-0x04, 0x06 and 0x0A
                    Ktw    Fixed             ESCAN00041708 UDS                            Wrong minimum length calculation for services without sub-function
                    Ktw    Fixed             ESCAN00041576 All                            Compiler warning for constant expression in DescDispatcher
2010-05-07  6.03.00
                    Ktw    Added             ESCAN00042840 Vector(UDS)                    Support ASR 3.0 interface for 0x19 0x0B-0x0E and 0x14
                    Ktw    Fixed             ESCAN00042963 All                            DescCopyToCan API does not support CAN message buffers located in the gpage
2010-26-07  6.03.01
                    Ktw    Fixed             ESCAN00044307 Vector(UDS)                    Rework of Dem ASR 3.0 interface
                    Ktw    Fixed             ESCAN00043240 UDS                            Compiler error for missing data types DescDynDidMemBlockAddress and DescDynDidMemBlockSize
                    Ktw    Fixed             ESCAN00044050 All                            Compiler warning: condition is always false
                    Ktw    Fixed             ESCAN00044118 All                            CANdesc task period not changeable
                    Ktw    Fixed             ESCAN00043273 All                            Unexpected generation error occurs
                    Ktw    Fixed             ESCAN00044197 All                            Not all buffers used in DANIS can be located in far RAM
2010-08-16  6.04.00
                    Ktw    Fixed             ESCAN00044701 Vector(UDS)                    Compiler warning for inconsistency of function definition and declaration, Dem Interface
                    Ktw    Fixed             ESCAN00045154 Vector(UDS)                    FAR Buffer handling is not consistent, Dem Interface
                    Ktw    Modified          ESCAN00045152 Vector(UDS)                    uintX datatype is used         
                    Ktw    Added             ESCAN00044266 Vector(UDS)                    Support ASR 3.0 interface for 0x19 0x07-0x09
                    Ktw    Added             ESCAN00044725 Vector(UDS)                    Support Service 0x2C           
                    Ktw    Fixed             ESCAN00044528 All                            Compile errors in DanisIsoTp communication adapter
                    Ktw    Fixed             ESCAN00044371 All                            v_inc.h is not included in the generated DANIS adapter template
2011-01-11  6.05.00
                    Ktw    Fixed             ESCAN00047096 Vector(UDS)                    Services 0x19 0x04/0x06 may not send positive response
                    Ktw    Fixed             ESCAN00047568 Vector(UDS)                    Compile warning: Wrong pointer type used for API call Dem_GetFreezeFrameDataIdentifierByDTC
                    Ktw    Fixed             ESCAN00047569 Vector(UDS)                    Compile warning: unreferenced local variable ringbufPtr
                    Ktw    Added             ESCAN00048042 Vector(UDS)                    Support Service 0x2A with configurable transmit type
                    Ktw    Fixed             ESCAN00045932 All                            Compile error on each usage of DescInterruptDisable/-Restore internal API
                    Ktw    Fixed             ESCAN00047741 All                            Adapt CANdesc TMS320 support for GENy enabled CANdriver
                    Ktw    Fixed             ESCAN00047355 All                            Memory mapping of FAR buffer support use incomplete description for paged memory �C
                    Ktw    Fixed             ESCAN00046410 All                            After ECU reset the diagnostic response is not transmitted after the function DescSendPosRespFBL has been called
2011-04-11  6.06.00
                    Ktw    Fixed             ESCAN00050678 Vector(UDS)                    Random data may get overwritten after request of the diagnostic service $19
                    Mhe    Modified          ESCAN00043238 UDS                            Communication Control will not compile with communication  parameters that have a certain value
                    Ktw    Fixed             ESCAN00050894 UDS                            Service 0x2C 0x02 with invalid ALFID leads to positive response without appending new items to the DynDID
                    Ktw    Fixed             ESCAN00049396 UDS                            No positive response sent if SPRMIB=TRUE and API DescForceRcrRpResponse is used
                    Ktw    Fixed             ESCAN00049396 UDS                            CANdesc does not reject requested service 0x2C 0x02 with ALFID==0x00
                    Mhe    Modified          ESCAN00036927 All                            Allow parallel OBD support if only the TPMC DANIS adapter is available
                    Mhe    Added             ESCAN00044127 All                            Support for DANIS adapter specific available buffer size information to the application
                    Mhe    Added             ESCAN00048157 All                            Add Danis adapter for LinTp    
                    Ktw    Fixed             ESCAN00048636 All                            Error message references non-existent file 'ApplDesc.h'
2011-08-12  6.07.00
                    Ktw    Fixed             ESCAN00051088 UDS                            Service 0x2A 0x04 ends up with endless RCR-RP responses on asynchronously read periodic DIDs
                    Ktw    Modified          ESCAN00051315 All                            API DescGetCurrentBusInfo should be usable for periodic transmission
                    Ktw    Added             ESCAN00052870 All                            Support Multiple Addressing (combination Normal and Extended Addressing)
2011-09-01  6.07.01
                    Ktw    Fixed             ESCAN00053203 Vector(UDS)                    Compile error: kDescNetAssertUnexpectedLength is not defined
                    Ktw    Fixed             ESCAN00053205 All                            Wrong function is called to get addressing type
2011-09-06  6.08.00
                    Ktw    Fixed             ESCAN00053468 Vector(UDS)                    Missing assertion define 'kDescAssertPageBufferTooSmallForResponse'
2011-09-24  6.08.01
                    Ktw    Fixed             ESCAN00053847 All                            No response is sent to a physical request
                    Ktw    Fixed             ESCAN00053042 All                            CANdesc sends UUDT responses with CAN IDs from other identities
2011-10-04  6.08.02
                    Ktw    Fixed             ESCAN00054014 Vector(UDS)                    Generator crash: Service 0x2a activated and no UUDT messages
                    Ktw    Added             ESCAN00054105 All                            Move some configuration defines of Service 0x2c and 0x2a to the header file
2011-10-25  6.09.00
                    Ktw    Added             ESCAN00055172 UDS                            Support means for spontaneous responses
2011-12-09  6.10.00
                    Ktw    Fixed             ESCAN00055009 UDS                            Already scheduled Did is denied as not supported
2011-12-29  6.10.01
                    -      -                 -             -                              No relevant changes available in this version.
2012-01-13  6.10.02
                    Ktw    Fixed             ESCAN00055791 UDS                            Not possible to switch of  functional addressing for Service 0x2c
2012-02-09  6.10.03
                    -      -                 -             -                              No relevant changes available in this version.
2012-02-23  6.11.00
                    Ktw    Fixed             ESCAN00057448 Vector(UDS)                    Not possible to configure a value for start on delay
                    Ktw    Modified          ESCAN00057403 Vector(UDS)                    Rework shaping of Service 0x19 if Dem is part of the system
                    Ktw    Modified          ESCAN00058012 Vector(UDS)                    Optimization  of  Dem interface for reading extended data record and snapshot data
                    Ktw    Added             ESCAN00058070 Vector(UDS)                    Added possibility to implement security access in the application
                    Ktw    Added             ESCAN00057614 All                            Better error message if an Oem is not supported by CANdesc but supported in the datamodel
2012-04-18  6.11.01
                    Ktw    Fixed             ESCAN00058417 Vector(UDS)                    Compile error: Desc1904GetSizeOfFreezeFrames wrong parameter list
2012-07-02  6.11.02
                    Ktw    Fixed             ESCAN00059114 Vector(UDS)                    Service 0x2a: not possible to add Pids to scheduler first with a functional and then with a physical request
2012-07-27  6.12.00
                    -      -                 -             -                              No relevant changes available in this version.
2012-10-27  6.12.01
                    Ktw    Modified          ESCAN00062366 All                            CANDesc on multiple channels: Support more than 4 channels 
2012-11-12  6.13.00
                    Ktw    Modified          ESCAN00065354 UDS                            Send a pending response for a request with SPRMIB and ring buffer usage for the response
2013-06-04  6.14.00
                    -      -                 -             -                              No relevant changes available in this version.
2013-09-05  6.14.01
                    Sa     Fixed             ESCAN00070242 UDS                            Unexpected behavior executing service 0x2A 0x04 with concrete PDID(s)
2013-09-27  6.15.00
                    Rpk    Fixed             ESCAN00058649 Vector(UDS)                    Security Access: False Attempt Counter is changed after delay timer is elapsed and not on valid key
2013-11-29  6.15.01
                    -      -                 -             -                              No relevant changes available in this version.
2014-01-10  6.15.02
                    -      -                 -             -                              No relevant changes available in this version.
2014-01-31  6.16.00
                    Rpk    Added             ESCAN00074628 Vector(UDS)                    Support DEM 412 Interface      
2014-07-14  6.17.00
                    Rpk    Fixed             ESCAN00077591 Vector(UDS)                    DescRingBufferWrite triggers an assert
                    Rpk    Fixed             ESCAN00077461 Vector(UDS)                    Compiler error: 'g_descUsedFrzFrameRecIds' is undefined
                    Rpk    Fixed             ESCAN00076931 Vector(UDS)                    Service 0x19: Continuous response pending messages without final response
                    Rpk    Fixed             ESCAN00076339 Vector(UDS)                    CANdesc calls DEM SetDTCFilter API with wrong DTC kind for service 0x19 0x15
                    Rpk    Fixed             ESCAN00076297 Vector(UDS)                    Unexpected response for service 0x19 0x05 0x00
                    Rpk    Fixed             ESCAN00075946 Vector(UDS)                    Compiler error: DescDemApiInterpretClearResult not defined
                    Rpk    Fixed             ESCAN00061617 All                            Compiler warning: warning C4244: '=' : conversion from 'DescDynDidMemBlockSize' to 'vuint16', possible loss of data
2014-08-12  6.17.01
                    Rpk    Fixed             ESCAN00077788 Vector(UDS)                    Compiler error: busProperty is not a member of t_descUudtNetBusInfoTag
2014-11-05  6.18.00
                    Rpk    Fixed             ESCAN00080018 All                            AR3 DEM interface: Activating the iteration limiter for reading DTCs from the DEM has no effect
                    Rpk    Fixed             ESCAN00080019 All                            AR4 DEM interface: Activating the iteration limiter for reading DTCs/SnapshotRecordIdentification has no effect
2015-06-23  6.19.00
                    Rpk    Fixed             ESCAN00083960 Vector(UDS)                    Correct comments and error directive in the Asr4 Dem Interface
                    Rpk    Fixed             ESCAN00081314 Vector(UDS)                    Compiler warning: DCM_RUNTIME_USAGE_LIMIT_ENABLED is not defined
                    Aey    Modified          ESCAN00084257 Vector(UDS)                    Service 0x19 0x05 0xFF shall return positive response for SSR 0x00
                    Aey    Modified          ESCAN00084246 Vector(UDS)                    Support DCM 4.2.1 Dem_DcmClearDTC return value DEM_CLEAR_MEMORY_ERROR
                    Aey    Fixed             ESCAN00084348 UDS                            Unexpected negative response for a Security Access request with NRC 0x36 or 0x24
                    Rpk    Fixed             ESCAN00083701 All                            Compiler error: non ASCII character used in comments
                    Rpk    Fixed             ESCAN00083450 All                            Generating unnecessary dummy DANIS TP adapter functions inside desc.c when there no TP and DANIS TP adapter are configured.
                    Rpk    Fixed             ESCAN00081716 All                            The diagnostic communication is blocked and a diagnostic response might be sent on the wrong CAN channel
2015-09-03  6.20.00
                    Aey    Added             ESCAN00085342 Vector(UDS)                    Unlock the API DescStartSecureTimer() for Vector OEM UDS2006
2015-10-23  6.21.00
                    Aey    Fixed             ESCAN00085340 UDS                            ECU may send empty positive response for service 0x19 0x05 0x00
                    Aey    Fixed             ESCAN00073624 UDS                            Service 0x19 0x04/0x06/0x10/0x18/0x19 with RecID 0xFF has different behavior depending on the PagedBuffer support
                    Aey    Fixed             ESCAN00086295 All                            Compiler warning: Infinite loop possibility
                    Aey    Fixed             ESCAN00072264 All                            candesc support of more than 8 logical channels
                    Aey    Added             ESCAN00087649 All                            Support for RcrRp in CANdesc - LinTP interface
2016-03-15  6.22.00
                    -      -                 -             -                              No relevant changes available in this version.
2016-05-19  6.23.00
                    Wse    Added             ESCAN00090258 Vector(UDS)                    Reset of attempt counter is configurable in GENy
                    Wse    Added             ESCAN00090212 Vector(UDS)                    Delete dynamical  defined DIDs when going back to default session configurable in GENy
                    Wse    Fixed             ESCAN00090447 UDS                            Replace NRC kDescNrcConditionsNotCorrec (0x22) by NRC kDescNrcRequestOutOfRange (0x31) in service $19 when using Vector DEM
                    Wse    Fixed             ESCAN00083566 All                            Compiler warning: variable g_descSchedulerTimer set but never used
                    Wse    Fixed             ESCAN00083588 All                            Compiler warning: variable g_descRdpiStateCtrl is set but never used
2016-07-28  6.24.00
                    Wse    Fixed             ESCAN00090646 UDS                            UUDT messages overload in combination with service $22 for DynDID
2016-10-05  6.25.00
                    Wse    Added             ESCAN00092202 Vector(UDS)                    Calling DescSendPosRespFBL possible for OEM=Vector
                    Wse    Fixed             ESCAN00091636 UDS                            Compiler warning: inconsistent storage class specification for Desc85Process
                    Wse    Fixed             ESCAN00092078 UDS                            Pointer out of bounds in Desc85Process
2016-12-20  6.26.00
                    Wse    Added             ESCAN00093006 Vector(UDS)                    API to application added if attempt counter value changes for OEM=Vector
                    Wse    Fixed             ESCAN00055957 All                            appdesc.c missing line feed (LF) after carriage return (CR) on some lines
                    Wse    Fixed             ESCAN00092436 All                            Compiler error: (Linker Error) Symbol DescPostRoutineControlByIdentifier is undefined
                    Wse    Fixed             ESCAN00093238 All                            Null pointer read-access for busy-repeat-request (NRC 0x21) response in case of parallel client requests
2017-03-06  6.27.00
                    Wse    Fixed             ESCAN00093265 All                            Unexpected NRC 0x21 (BusyRepeatRequest) response sent to tester
2017-05-02  6.28.00
                    Wse    Fixed             ESCAN00080010 UDS                            Compiler error: DescInterruptDisable/DescInterruptRestore is not an expression
                    Wse    Fixed             ESCAN00095088 UDS                            service $22 rejected for supported DID in case of unified PID handling
                    Wse    Fixed             ESCAN00081861 All                            Compiler warning: Truncating assignment in DescContextStateTask
                    Wse    Fixed             ESCAN00081853 All                            Compiler warning: Truncating assignment in DescPidDispatcher
                    Wse    Fixed             ESCAN00081852 All                            Compiler warning: Truncating assignment in DescUudtNetCANTxReserveResource
                    Wse    Fixed             ESCAN00081850 All                            Compiler warning: Truncating assignment in DescICNGetResponseData
                    Wse    Fixed             ESCAN00081836 All                            Compiler warning: Truncating assignment in DescConfirmation
                    Wse    Fixed             ESCAN00055156 All                            Compiler warning: truncating assignment
                    Wse    Fixed             ESCAN00081827 All                            Compiler warning: Truncating assignment in DescUsdtNetIsoTpCopyToCan
2017-07-24  6.28.01
                    Wse    Fixed             ESCAN00095783 UDS                            CANdesc: array access out of bounds in function DescUudtNetCANCancelAllResponses()
2017-10-18  6.29.00
                    Wse    Added             ESCAN00097004 UDS                            DescSetNegResponse can be called in ApplDescSecurityAccessGetSeed and ApplDescSecurityAccessCheckKey
2018-03-21  6.30.00
                    Wse    Fixed             ESCAN00097832 All                            Response for service $22 incomplete in case of ring buffer used and multiple DIDs in request
                    Wse    Fixed             ESCAN00097834 All                            Handling of Gaps in a DID is not correct
                    Wse    Fixed             ESCAN00097915 All                            Response for service $22 incomplete if requested DID is handled as signal handler
                    Wse    Fixed             ESCAN00098423 All                            Response for service $22 incomplete in case of multiple DIDs in request using ring buffer mechanisms
                    Wse    Added             ESCAN00097837 All                            Variable handling of buffer size to support CAN-FD
2018-06-06  6.31.00
                    Wse    Fixed             ESCAN00099512 All                            NRC=0x78 is sent forever after request $19 04, $19 06 or $19 10
                    Wse    Added             ESCAN00098571 All                            Spontaneous messages in CAN-FD 
------------------------------------------------------------------------------------------------------------------------
 */


/* -----------------------------------------------------------------------------
    &&&~ Includes
 ----------------------------------------------------------------------------- */

/* Physical layer specific API */
/* Check if CCL is available and take into account its configuration */
#include "CAEA_Types.h"
#include "CanApp.h"
#include "core_common.h"
#include "device_registers.h"
//#include "flexcan_driver.h"
#include "il_def.h"
#include "status.h"
//#include "trng_config.h"
#include <stdint.h>
#if defined (VGEN_ENABLE_CCL)
# include "ccl_cfg.h"
#endif
#include "Can_Fbl.h"
//#include "HwIO.h"
/* Check if CCLcom is available and take into account its configuration */
#if defined (VGEN_ENABLE_CCLCOM)
# include "cclcom_cfg.h"
#endif

/* Make all CANdesc specific typedefs available */
#include "desc.h"
/* Include the implementation prototypes for prototype checks */
#include "appdesc.h"

/* Required for the implementation of ApplDescInitPosResFblBusInfo */
#include "tpmc.h"
//#include "SpiIf.h"

#include "CanDem.h"

#include "CanDem_Cfg.h"
#include "CanDem.h"
#include "Appl_DID_Callback.h"
#include "Appl_CPID_Callback.h"
#include "Appl_PID_Callback.h"
#include "Random.h"
//#include "STD_BtrDiag_Cfg.h"
//#include "system_YTM32B1MC0.h"
#include "CanApp.h"
#include "DID_Callback.h"
#include "Can_Fbl.h"
#include "STD_AdcMCfg.h"
// #include "ptmr_driver.h"
// #include "ptmr_config.h"
#include "SysM_Cfg.h"
#include "STD_BtrCtr.h"
/* -----------------------------------------------------------------------------
    &&&~ Preprocessor constants (defines)
 ----------------------------------------------------------------------------- */
#define kDescProgramming_Flag                                        0x55385493
#define kDescEcuCryptoKey                                            0x12345678

#define CeDemIdentifiersNumber 1u
#define CeDemSnapRecordNumber_onlySupport  1u

/* -----------------------------------------------------------------------------
    &&&~ Function prototypes
 ----------------------------------------------------------------------------- */

static vuint32 SecM_ComputeKey(void);
static void SecM_Serializer(DescMsg tgtPtr, vuint32 data);



/* -----------------------------------------------------------------------------
    &&&~ RAM definitions
 ----------------------------------------------------------------------------- */

/* 64Bit seed stored for the later key compare */
 vuint32 g_applDescSeedX;
//static vuint32 g_applDescSeedY;
static vuint32 reprogrammingRequest = 0U;
vuint8 AppDesc_28Service_Ctrl_Flag = 0u;
vuint8 AppDesc_28Service_CtrlNM_Flag = 0u;
vuint8 ProgPrecFlag0203 = FALSE;


//static vuint8 ApplSecurityAccessGetSeedFlag = 0u;
/* -----------------------------------------------------------------------------
    &&&~ Implementation
 ----------------------------------------------------------------------------- */

#if defined (DESC_ENABLE_DEBUG_USER ) || defined (DESC_ENABLE_DEBUG_INTERNAL)
# if defined (CCL_ENABLE_ERROR_HOOK) || defined (CCLCOM_ENABLE_ERROR_HOOK)
/* CCL takes care about this function */
# else
/* ********************************************************************************
 * Function name:ApplDescFatalError
 * Description: This function will be called each time while the debug mode is active a
 * CANdesc fault has been detected. If you reach this function it makes no sence to continue the tests since CANdesc
 * will not operate properly until next start of the ECU.
 * Returns:  nothing
 * Parameter(s):
 *   - errorCode:
 *       - The assert code text equivalent can be found in desc.h (kDescAssert....).
 *       - Access type: read
 *   - lineNumber:
 *       - Since the same fault could be cales on many places the line number shows where exactly it occured.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - Set a break point at this place to know during the ECU development if you ran onto it.
 ******************************************************************************** */
void DESC_API_CALLBACK_TYPE ApplDescFatalError(vuint8 errorCode, vuint16 lineNumber)
{
  /*<<TBD>> Remove this comment once you have completely implemented this function!!!*/
  /* Avoid warnings */
  DESC_IGNORE_UNREF_PARAM(errorCode);
  DESC_IGNORE_UNREF_PARAM(lineNumber);

  /* When fatal error occurs, cause an ECU hang up at this point.
   * Please set break point at this line to investigate both parameter values. */
  for(;;);
}
# endif
#endif

#if defined (DESC_ENABLE_SPONTANEOUS_RES)
void DESC_API_CALLBACK_TYPE ApplDescSpontaneousResponseConfirmation(DESC_CONTEXT_FORMAL_PARAM_DEF_FIRST vuint8 status)
{
  /*<<TBD>> Remove this comment once you have completely implemented this function!!!*/
    /* Avoid warnings */
  DESC_CONTEXT_PARAM_DUMMY_USE;

  /* Check the transmission status */
  if(status == kDescOk)
  {
    /* "Spontaneous Response" was just successfully sent.
     */
  }
  else
  {
    /* There was some transmission error and the tester didn't received the "Spontaneous Response" response.
     */
  }
}
#endif

/*******************************************************************************
* NAME:              ApplDescInitPosResFblBusInfo
*
* CALLED BY:         CANdesc
* PRECONDITIONS:
*
* DESCRIPTION:       Initializes the busInfo with the concrete response address information
*                    (e.g. sourceAddr,TargetAddr, COM channel, etc.).
*******************************************************************************/
vuint8 DESC_API_CALL_TYPE ApplDescInitPosResFblBusInfo(t_descUsdtNetBus* pBusInfo)
{
  /* Init the bus-info structure as shown below */
  pBusInfo->busType = kDescUsdtNetBusTypeIsoTp;/* on other bus systems use the apropriate bus-type */
  /*On multi channel systems */
  pBusInfo->comChannel = 0; /* set the CAN (FlexRay or other) channel on which to send */
  /* If the addressing scheme uses source/target addresses put here the tester addres to respond to */
  pBusInfo->testerId = 0xF1; /* set the CAN (FlexRay or other) channel on which to send */

#if defined(DESC_USDTNET_ENABLE_DYNAMIC_BUFFER_LENGTH)
  /* If dynamic length is used, please use the correct buffer size of the corresponding DANIS driver!!! */
  pBusInfo->availBufferLength = kDescPrimBufferLen;
#endif

#if defined(TP_ENABLE_DYNAMIC_CHANNELS) && defined(TP_ENABLE_CANFD)
  /* If response shall be set as CAN-FD message please modify the value */
  pBusInfo->canType = kTpCAN20;
#endif

#if ( defined (TP_ENABLE_DYNAMIC_CHANNELS) && \
    (defined(TP_ENABLE_MIXED_11_ADDRESSING) || \
    defined(TP_ENABLE_NORMAL_ADDRESSING)) )
  /* ********** for these use cases additional TPMC initalization is required ********** */
  {
    vuint8 tpChannel = kTpTxChannelCount;
    while (tpChannel != 0)
    {
      tpChannel--;
      if (kDescDiagConnection == TpTxGetConnectionNumber(tpChannel))
      {
# if defined(TP_ENABLE_MULTIPLE_ADDRESSING)
        tpTxInfoStruct[tpChannel].assignedDestination = kTpRequestDiagPhysical;
        tpTxInfoStruct[tpChannel].addressingFormat = kTpNormalAddressing; /*... respectively: kTpMixed11Addressing, kTpExtendedAddressing*/
# endif

//# error "Replace the <DIAG_NORMAL_ADD_RES_ID> and <DIAG_NORMAL_ADD_REQ_ID> below with the real CANids and uncomment the line!"
        /* TpTxSetChannelID(tpChannel, <DIAG_NORMAL_ADD_RES_ID>, <DIAG_NORMAL_ADD_REQ_ID>); */
# if (kTpNumberOfCanChannels > 1)
        TpTxSetCanChannel(tpChannel, pBusInfo->comChannel);
# endif
        break;
      }
    }
  }
#endif
  return kDescOk;
}

/*  ********************************************************************************
 * Function name:ApplDescRcrRpConfirmation
 * Description:Will be called only if "DescForceRcrRpResponse" has been previously called.
 * Returns:  nothing
 * Parameter(s):
 *   - status:
 *       - Current RCR-RP transmission status (kDescOk/kDescFailed).
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" may be called to close the service processing.
 *   - The function "DescSetNegResponse" may be called to set a negative response.
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescRcrRpConfirmation(vuint8 status)
{
	//vuint8 Ca_Test[12] = "CAEA--U1EI21";
    static uint32 g_temp = 0u;
    if ( kDescProgramming_Flag == reprogrammingRequest )
    {
        if (kTpSuccess == status)
        {         
					reprogrammingRequest = 0u;
			
          for(g_temp = 0u;g_temp < 10000u;g_temp++)
          {
            NOP();
          }

          SysM_MCUDeinit();

          FblStart();
          //CallFblStart(1);
        }
    }
}


/*  ********************************************************************************
 * Function name:ApplDescOnTransitionSession
 * Description:Notification function for state change of the given state group, defined by
 * CANdelaStudio.
 * Returns:  nothing
 * Parameter(s):
 *   - newState:
 *       - The state which will be set.
 *       - Access type: read
 *   - formerState:
 *       - The current state of this state group.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" may not be called.
 *   - The function "DescSetNegResponse" may not be called.
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescOnTransitionSession(DescStateGroup newState, DescStateGroup formerState)
{
    /* <<TBD>> Remove this comment once you have completely implemented this function!!! */
    /* This is only a notification function. Using the "formerState" and the "newState" 
    * parameter you can distinguish the different transitions for this state group.
    */
    /* Avoids warnings */
    if ( ( kDescStateSessionDefault == newState ) && (kDescStateSessionExtended == formerState) ) /* 10 01 */
    {
       /// (void)CPID_CAEAReturnCtlToECU();
        CanDem_DcmEnableDTCSetting();
		DescEnableCommunication();
		if(AppDesc_28Service_Ctrl_Flag == 1u)
		{
			(void)Nm_EnableCommunication(0u);
			IlTxStart();
			IlRxStart();
			AppDesc_28Service_Ctrl_Flag = 0u;
		}

    		if(AppDesc_28Service_CtrlNM_Flag == 1u)
		{
			(void)Nm_EnableCommunication(0u);
		//	IlTxStart();
		//	IlRxStart();
			AppDesc_28Service_CtrlNM_Flag = 0u;
		}



    }
    DESC_IGNORE_UNREF_PARAM(formerState);
}


/*  ********************************************************************************
 * Function name:ApplDescOnTransitionSecurityAccess
 * Description:Notification function for state change of the given state group, defined by
 * CANdelaStudio.
 * Returns:  nothing
 * Parameter(s):
 *   - newState:
 *       - The state which will be set.
 *       - Access type: read
 *   - formerState:
 *       - The current state of this state group.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" may not be called.
 *   - The function "DescSetNegResponse" may not be called.
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescOnTransitionSecurityAccess(DescStateGroup newState, DescStateGroup formerState)
{
  /* <<TBD>> Remove this comment once you have completely implemented this function!!! */
   /* This is only a notification function. Using the "formerState" and the "newState" 
   * parameter you can distinguish the different transitions for this state group.
   */
  /* Avoids warnings */
  DESC_IGNORE_UNREF_PARAM(newState);
  DESC_IGNORE_UNREF_PARAM(formerState);
;
}


/*  ********************************************************************************
 * Function name:ApplDescEcuResetHard (Service request header:$11 $1 )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescEcuResetHard(DescMsgContext* pMsgContext)
{
	
	if((1 == PendingVehicleSpeed()) &&(1 == PendingPDCM_actualGear()))
	{
		DescProcessingDone();

	}
	else
	{	
		DescSetNegResponse(0x22); 
		DescSessionTransitionChecked(DESC_CONTEXT_PARAM_ONLY);
	}

}


/*  ********************************************************************************
 * Function name:ApplDescEcuResetSoft (Service request header:$11 $3 )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescEcuResetSoft(DescMsgContext* pMsgContext)
{
	
	if((1 == PendingVehicleSpeed()) &&(1 == PendingPDCM_actualGear()))
	{
		DescProcessingDone();

	}
	else
	{	
		DescSetNegResponse(0x22); 
		DescSessionTransitionChecked(DESC_CONTEXT_PARAM_ONLY);
	}
}






/*  ********************************************************************************
 * Function name:ApplDescClearDiagInfo (Service request header:$14 )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescClearDiagInfo(DescMsgContext* pMsgContext)
{
    vuint32 LeAppdesc_u_Temp;
    
	

	
    LeAppdesc_u_Temp  = ( (vuint32)pMsgContext->reqData[0] ) << 16U;
    LeAppdesc_u_Temp |= ( (vuint32)pMsgContext->reqData[1] ) << 8U;
    LeAppdesc_u_Temp |= (vuint32)pMsgContext->reqData[2];
    if( CanDem_u_Ok == CanDem_DcmClearDTCSignal(LeAppdesc_u_Temp))
    {
        pMsgContext->resDataLen = 0U;
        pMsgContext->resData[0] = 0xFF;
        pMsgContext->resData[1] = 0xFF;
        pMsgContext->resData[2] = 0xFF;
    }
    else
    {
        /* Request contains invalid data - send negative response! */
        DescSetNegResponse(( DescNegResCode )kDescNrcRequestOutOfRange);
    }
	
	if((1 == PendingVehicleSpeed()) &&(1 == PendingPDCM_actualGear()))
	{
		//	DescProcessingDone();
	}
	else
	{	
		DescSetNegResponse(0x22); 
	}
	
	
    /* User service processing finished. */
    DescProcessingDone();
}


/*  ********************************************************************************
 * Function name:ApplDescReadDtcRNODTCBSM (Service request header:$19 $1 )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescReadDtcRNODTCBSM(DescMsgContext* pMsgContext)
{
    uint8 LeAppdesc_u_StatusMask;
    uint8 LeAppdesc_u_RequestMask;
    uint16 LeAppdesc_u_MaskNumber;
    
    LeAppdesc_u_RequestMask = *(pMsgContext->reqData);
    
	
	if((LeAppdesc_u_RequestMask == 0x09) || (LeAppdesc_u_RequestMask == 0x01) ||(LeAppdesc_u_RequestMask == 0x08)||(LeAppdesc_u_RequestMask == 0xFF))       
	{
		(void)CanDem_DcmGetDTCStatusAvailabilityMask(&LeAppdesc_u_StatusMask);

		pMsgContext->resDataLen = 4u;
		pMsgContext->resData[0] = 0x09;  /*ycl 20.04.29*/
		pMsgContext->resData[1] = 0x00;/*Dem_DcmGetTranslationType();*/
		
		
		
		
		
		(void)CanDem_DcmGetNumberOfFilteredDTC(&LeAppdesc_u_MaskNumber,LeAppdesc_u_RequestMask);
		
		pMsgContext->resData[2] = (uint8)(LeAppdesc_u_MaskNumber >> 8U);
		pMsgContext->resData[3] = (uint8)LeAppdesc_u_MaskNumber;
	}
	else
	{
		DescSetNegResponse(( DescNegResCode )kDescNrcRequestOutOfRange);
	}
	

    /* User service processing finished. */
    DescProcessingDone();
}


/*  ********************************************************************************
 * Function name:ApplDescReadDtcRDTCBSM (Service request header:$19 $2 )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescReadDtcRDTCBSM(DescMsgContext* pMsgContext)
{
    uint8  LeAppdesc_u_StatusMask;
    uint16 LeAppdesc_w_Index;
    uint16 LeAppdesc_w_DTCNumberIndex;
    uint8  LeAppdesc_u_RequestMask;
    
    uint16 LeAppdesc_u_MaskNumber;
    
    LeAppdesc_u_RequestMask = pMsgContext->reqData[0];  


    if((LeAppdesc_u_RequestMask == 0x09) || (LeAppdesc_u_RequestMask == 0x01) ||(LeAppdesc_u_RequestMask == 0x08)||(LeAppdesc_u_RequestMask == 0xFF))       
    {
       (void)CanDem_DcmGetDTCStatusAvailabilityMask(&LeAppdesc_u_StatusMask);  
    
      LeAppdesc_w_Index       = 1U;
      pMsgContext->resDataLen = LeAppdesc_w_Index;
      //pMsgContext->resData[LeAppdesc_w_Index - 1U] = 0x7F; /*ycl 20.04.29*/
      pMsgContext->resData[LeAppdesc_w_Index - 1U] = LeAppdesc_u_StatusMask; /*ycl 20.04.29*/
	
	
     (void)CanDem_DcmGetNumberOfFilteredDTC(&LeAppdesc_u_MaskNumber,(uint8)(LeAppdesc_u_RequestMask & Can_u_StatusAvailabilityMask));
    
      for(LeAppdesc_w_DTCNumberIndex =0;LeAppdesc_w_DTCNumberIndex<LeAppdesc_u_MaskNumber;LeAppdesc_w_DTCNumberIndex++)
      {
        uint32 LeAppdesc_u_DTCNumber;
        uint8  LeAppdesc_u_DTCStatus;
        
        (void)CanDem_DcmGetNextFilteredDTC(&LeAppdesc_u_DTCNumber, &LeAppdesc_u_DTCStatus,LeAppdesc_w_DTCNumberIndex);
        pMsgContext->resData[LeAppdesc_w_Index] = (uint8)((LeAppdesc_u_DTCNumber >> 16U) & 0xFFU);
        pMsgContext->resData[LeAppdesc_w_Index + 1U] = (uint8)((LeAppdesc_u_DTCNumber >> 8U) & 0xFFU);
        pMsgContext->resData[LeAppdesc_w_Index + 2U] = (uint8)(LeAppdesc_u_DTCNumber & 0xFFU);
        if(LeAppdesc_u_DTCStatus == 0x00)/*ycl 20.04.29*/
        {
            
        }
        else
        {
            pMsgContext->resData[LeAppdesc_w_Index + 3U] = LeAppdesc_u_DTCStatus ;   /*ycl 20.04.29*/
        }

        LeAppdesc_w_Index  += 4U;
        pMsgContext->resDataLen = LeAppdesc_w_Index;
     }       
    }
  else
	{
		DescSetNegResponse(( DescNegResCode )kDescNrcRequestOutOfRange);
	}
   
    /* User service processing finished. */
    DescProcessingDone();
}


/*  ********************************************************************************
 * Function name:ApplDescReadDtcRDTCSSBDTC (Service request header:$19 $4 )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */

void DESC_API_CALLBACK_TYPE ApplDescReadDtcRDTCSSBDTC(DescMsgContext* pMsgContext)
{

	uint8   LeAppdesc_u_DTCVaild_Flag = 0;
	uint32   LeAppdesc_dw_DTCNumber;
    uint8    LeAppdesc_u_DTCStatus;
    uint16   LeAppdesc_dw_DTCIndex;
	
	uint16 SnapShotData_Speed;
	uint32 SnapShotData_Odometer;
	
    

    LeAppdesc_dw_DTCNumber  = ((uint32)(pMsgContext->reqData[0]) << 16u);
    LeAppdesc_dw_DTCNumber |= ((uint32)(pMsgContext->reqData[1]) << 8u);
    LeAppdesc_dw_DTCNumber |= (uint32)pMsgContext->reqData[2];

    LeAppdesc_u_DTCVaild_Flag = CanDem_DcmGetStatusOfDTC(LeAppdesc_dw_DTCNumber,0U,&LeAppdesc_u_DTCStatus);
    if( ( LeAppdesc_u_DTCVaild_Flag == CanDem_u_Ok ) && ((pMsgContext->reqData[3] == CeDemSnapRecordNumber_onlySupport)
            ||(pMsgContext->reqData[3] == 0xFF))) 
    {
        LeAppdesc_dw_DTCIndex = CanDem_DcmFindEventFromDtcInfo(LeAppdesc_dw_DTCNumber,0U);

        pMsgContext->resDataLen = 4U;
        pMsgContext->resData[0] = pMsgContext->reqData[0];
        pMsgContext->resData[1] = pMsgContext->reqData[1];
        pMsgContext->resData[2] = pMsgContext->reqData[2];
        
        if( (LeAppdesc_u_DTCStatus & Can_u_StatusAvailabilityMask)!= 0u)/*ycl 20.04.29*/
        {
            pMsgContext->resData[3] = LeAppdesc_u_DTCStatus ; /*ycl 20.04.29*/
        }
        else
        {
            pMsgContext->resData[3] = 0; 
        }   
		
        if( (LeAppdesc_u_DTCStatus & Can_u_StatusAvailabilityMask)!= 0u)
        {	
          SnapShotData_Speed = SeDem_u_DTCInfo[LeAppdesc_dw_DTCIndex].DTCSnapShotData.e_u_Speed;
          SnapShotData_Odometer = SeDem_u_DTCInfo[LeAppdesc_dw_DTCIndex].DTCSnapShotData.e_u_OdometerValue;

          pMsgContext->resDataLen   = 20U;
          pMsgContext->resData[4]   = CeDemSnapRecordNumber_onlySupport;
          pMsgContext->resData[5]   = CeDemIdentifiersNumber;
          pMsgContext->resData[6]   = 0xF1U;
          pMsgContext->resData[7]   = 0xF8U;
          pMsgContext->resData[8]   = (uint8)((SnapShotData_Odometer >> 16) & 0xFF);
          pMsgContext->resData[9]   = (uint8)((SnapShotData_Odometer >> 8) & 0xFF);
          pMsgContext->resData[10]  = (uint8)(SnapShotData_Odometer & 0xFF);
          pMsgContext->resData[11]  = (uint8)(SeDem_u_DTCInfo[LeAppdesc_dw_DTCIndex].DTCSnapShotData.e_u_ECU_Voltage);
          pMsgContext->resData[12]  = (uint8)((SnapShotData_Speed >> 8) & 0xFF);
          pMsgContext->resData[13]  = (uint8)( SnapShotData_Speed & 0xFF);
          pMsgContext->resData[14]  = (uint8)(SeDem_u_DTCInfo[LeAppdesc_dw_DTCIndex].DTCSnapShotData.e_u_Sec);
          pMsgContext->resData[15]  = (uint8)(SeDem_u_DTCInfo[LeAppdesc_dw_DTCIndex].DTCSnapShotData.e_u_Min);
          pMsgContext->resData[16]  = (uint8)(SeDem_u_DTCInfo[LeAppdesc_dw_DTCIndex].DTCSnapShotData.e_u_Hour);
          pMsgContext->resData[17]  = (uint8)(SeDem_u_DTCInfo[LeAppdesc_dw_DTCIndex].DTCSnapShotData.e_u_Day);
          pMsgContext->resData[18]  = (uint8)(SeDem_u_DTCInfo[LeAppdesc_dw_DTCIndex].DTCSnapShotData.e_u_Month);
          pMsgContext->resData[19]  = (uint8)(SeDem_u_DTCInfo[LeAppdesc_dw_DTCIndex].DTCSnapShotData.e_u_Year);
        }
        else
        {
          ;
        }
    }
    else
    {
        DescSetNegResponse(( DescNegResCode )kDescNrcRequestOutOfRange);
    }
    /* User service processing finished. */
    DescProcessingDone();
}


/*  ********************************************************************************
 * Function name:ApplDescReadDtcRSUPDTC (Service request header:$19 $A )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescReadDtcRSUPDTC(DescMsgContext* pMsgContext)
{
    uint8  LeAppdesc_u_StatusMask;
    uint16 LeAppdesc_w_Index;
    uint16 LeAppdesc_u_DtcNumber = (CanDem_u_DtcNumber - CanDem_u_DisRepNumber);  
    
    (void)CanDem_DcmGetDTCStatusAvailabilityMask(&LeAppdesc_u_StatusMask);
    
    LeAppdesc_w_Index       = 1U;
    pMsgContext->resDataLen = LeAppdesc_w_Index;
    pMsgContext->resData[LeAppdesc_w_Index - 1U] = LeAppdesc_u_StatusMask;
    
    while(LeAppdesc_u_DtcNumber > 0u)
    {
        uint32 LeAppdesc_u_DTCCode;
        uint8  LeAppdesc_u_DTCStatus;
        
        if(DsDem_h_ManagementUnit[LeAppdesc_u_DtcNumber-1].e_u_DtcSupport == TRUE)
        {
            CanDem_DcmGetAllSupportDTC(&LeAppdesc_u_DTCCode, &LeAppdesc_u_DTCStatus, LeAppdesc_u_DtcNumber-1U);
            pMsgContext->resData[LeAppdesc_w_Index] = (uint8)((LeAppdesc_u_DTCCode >> 16U) & 0xFFU);
            pMsgContext->resData[LeAppdesc_w_Index + 1U] = (uint8)((LeAppdesc_u_DTCCode >> 8U) & 0xFFU);
            pMsgContext->resData[LeAppdesc_w_Index + 2U] = (uint8)(LeAppdesc_u_DTCCode & 0xFFU);
            if( (LeAppdesc_u_DTCStatus & Can_u_StatusAvailabilityMask)!= 0u)/*ycl 20.04.29*/
            {
                pMsgContext->resData[LeAppdesc_w_Index + 3U] = LeAppdesc_u_DTCStatus;   /*ycl 20.04.29*/
            }
            else
            {
                pMsgContext->resData[LeAppdesc_w_Index + 3U] = 0;
            }
            LeAppdesc_w_Index       += 4U;
            pMsgContext->resDataLen = LeAppdesc_w_Index;
        }
        
        LeAppdesc_u_DtcNumber--;
    }
    
    DescProcessingDone();

}



void DESC_API_CALLBACK_TYPE ApplDescReadMemoryByAddress(DescMsgContext* pMsgContext)
{
	vuint8 LeApplDesc_u_RetCode;
 
	LeApplDesc_u_RetCode = ReadMemoryByAddress_23( pMsgContext );		

	if( kDescNrcNone == LeApplDesc_u_RetCode )
	{
         ;
	}
	else
	{   
		DescSetNegResponse( (DescNegResCode )LeApplDesc_u_RetCode ); 
	} 
	DescProcessingDone();
}


void DESC_API_CALLBACK_TYPE ApplDescWriteMemoryByAddress(DescMsgContext* pMsgContext)
{
    vuint8 LeApplDesc_u_RetCode;
    
    // 调用写内存实现函数
    LeApplDesc_u_RetCode = WriteMemoryByAddress_3D(pMsgContext);
    
    if(kDescNrcNone == LeApplDesc_u_RetCode)
    {
        ;
    }
    else
    {
        // 写入失败，设置负响应码
        DescSetNegResponse((DescNegResCode)LeApplDesc_u_RetCode);
    }
    
    DescProcessingDone();
}

/*  ********************************************************************************
 * Function name:ApplDescWriteDidDID_0xF089 (Service request header:$2E $F0 $89 )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescWriteDidDID_0xF089(DescMsgContext* pMsgContext)
{
	uint8 LeDCM_u_DIDRetVal;

    LeDCM_u_DIDRetVal = DID_WriteDid0xF089( pMsgContext->reqData );
    if( CeDID_OK == LeDCM_u_DIDRetVal )
	{
		pMsgContext->resDataLen = 0x00u;
		DescProcessingDone();
	}
	else if( CeDID_OutRange  == LeDCM_u_DIDRetVal )
	{
		DescSetNegResponse(kDescNrcRequestOutOfRange  );
		DescProcessingDone();
	}
	else if( CeDID_OutTime  == LeDCM_u_DIDRetVal )
	{
		DescSetNegResponse(kDescNrcGeneralProgrammingFailure  );
		DescProcessingDone();
	}
	else
	{
		DescStartRepeatedServiceCall (ApplDescWriteDidDID_0xF089);
	}
}


/*  ********************************************************************************
 * Function name:ApplDescWriteDidDID_0xF187 (Service request header:$2E $F1 $87 )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescWriteDidDID_0xF187(DescMsgContext* pMsgContext)
{
	uint8 LeDCM_u_DIDRetVal;

    LeDCM_u_DIDRetVal = DID_WriteDid0xF187( pMsgContext->reqData );
    if( CeDID_OK == LeDCM_u_DIDRetVal )
	{
		pMsgContext->resDataLen = 0x00u;
		DescProcessingDone();
	}
	else if( CeDID_OutRange  == LeDCM_u_DIDRetVal )
	{
		DescSetNegResponse(kDescNrcRequestOutOfRange  );
		DescProcessingDone();
	}
	else if( CeDID_OutTime  == LeDCM_u_DIDRetVal )
	{
		DescSetNegResponse(kDescNrcGeneralProgrammingFailure  );
		DescProcessingDone();
	}
	else
	{
		DescStartRepeatedServiceCall (ApplDescWriteDidDID_0xF187);
	}
}



/*  ********************************************************************************
 * Function name:ApplDescWriteDidDID_0xF18C (Service request header:$2E $F1 $8C )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescWriteDidDID_0xF18C(DescMsgContext* pMsgContext)
{
	uint8 LeDCM_u_DIDRetVal;

    LeDCM_u_DIDRetVal = DID_WriteDid0xF18C( pMsgContext->reqData );
    if( CeDID_OK == LeDCM_u_DIDRetVal )
	{
		pMsgContext->resDataLen = 0x00u;
		DescProcessingDone();
	}
	else if( CeDID_OutRange  == LeDCM_u_DIDRetVal )
	{
		DescSetNegResponse(kDescNrcRequestOutOfRange  );
		DescProcessingDone();
	}
	else if( CeDID_OutTime  == LeDCM_u_DIDRetVal )
	{
		DescSetNegResponse(kDescNrcGeneralProgrammingFailure  );
		DescProcessingDone();
	}
	else
	{
		DescStartRepeatedServiceCall (ApplDescWriteDidDID_0xF18C);
	}
}


/*  ********************************************************************************
 * Function name:ApplDescWriteDidDID_0xC001_CAEA_FEC_Count (Service request header:$2E $C0 $1 )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescWriteDidDID_0xF1F0(DescMsgContext* pMsgContext)
{
	uint8 LeDCM_u_DIDRetVal;

    LeDCM_u_DIDRetVal = DID_WriteDid0xF1F0( pMsgContext->reqData );
    if( CeDID_OK == LeDCM_u_DIDRetVal )
	{
		pMsgContext->resDataLen = 0x00u;
		DescProcessingDone();
	}
	else if( CeDID_OutRange  == LeDCM_u_DIDRetVal )
	{
		DescSetNegResponse(kDescNrcRequestOutOfRange  );
		DescProcessingDone();
	}
	else if( CeDID_OutTime  == LeDCM_u_DIDRetVal )
	{
		DescSetNegResponse(kDescNrcGeneralProgrammingFailure  );
		DescProcessingDone();
	}
	else
	{
		DescStartRepeatedServiceCall (ApplDescWriteDidDID_0xF1F0);
	}
}


/*  ********************************************************************************
 * Function name:ApplDescWriteDidDID_0xC002_Part_Number (Service request header:$2E $C0 $2 )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescWriteDidDID_0xF1F6(DescMsgContext* pMsgContext)
{
	uint8 LeDCM_u_DIDRetVal;

    LeDCM_u_DIDRetVal = DID_WriteDid0xF1F6( pMsgContext->reqData );
    if( CeDID_OK == LeDCM_u_DIDRetVal )
	{
		pMsgContext->resDataLen = 0x00u;
		DescProcessingDone();
	}
  else if( 0x22  == LeDCM_u_DIDRetVal )
	{
		DescSetNegResponse(kDescNrcConditionsNotCorrect  );
		DescProcessingDone();
	}
	else
	{
		;
	}
}


/*  ********************************************************************************
 * Function name:ApplDescWriteDidDID_0xF1FA (Service request header:$2E $F1 $FA )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescWriteDidDID_0xF1FA(DescMsgContext* pMsgContext)
{
	uint8 LeDCM_u_DIDRetVal;

    LeDCM_u_DIDRetVal = DID_WriteDid0xF1FA( pMsgContext->reqData );
    if( CeDID_OK == LeDCM_u_DIDRetVal )
	{
		pMsgContext->resDataLen = 0x00u;
		DescProcessingDone();
	}
  else if( 0x31  == LeDCM_u_DIDRetVal )
	{
		DescSetNegResponse(0x31  );
		DescProcessingDone();
	}
	else
	{
		;
	}
}

/*  ********************************************************************************
 * Function name:ApplDescWriteDidDID_0xF1FC (Service request header:$2E $F1 $FC )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescWriteDidDID_0xF1FC(DescMsgContext* pMsgContext)
{
	uint8 LeDCM_u_DIDRetVal;

    LeDCM_u_DIDRetVal = DID_WriteDid0xF1FC( pMsgContext->reqData );
    if( CeDID_OK == LeDCM_u_DIDRetVal )
	{
		pMsgContext->resDataLen = 0x00u;
		DescProcessingDone();
	}
	else if( CeDID_OutRange  == LeDCM_u_DIDRetVal )
	{
		DescSetNegResponse(kDescNrcRequestOutOfRange  );
		DescProcessingDone();
	}
	else if( CeDID_OutTime  == LeDCM_u_DIDRetVal )
	{
		DescSetNegResponse(kDescNrcGeneralProgrammingFailure  );
		DescProcessingDone();
	}
	else
	{
		DescStartRepeatedServiceCall (ApplDescWriteDidDID_0xF1FC);
	}
}

/*  ********************************************************************************
 * Function name:ApplDescWriteDidDID_0xC007_UI_Version (Service request header:$2E $C0 $7 )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescIoCtrlRetCtrlToEcuCPID_F1F1(DescMsgContext* pMsgContext)
{
  /* <<TBD>> Remove this comment once you have completely implemented this function!!! */
  /* Contains no request data */
  /* Contains no response data */
  /* User service processing finished. */
  DescProcessingDone();
}


/*  ********************************************************************************
 * Function name:ApplDescIoCtrlShortTermAdjCPID_F1F1 (Service request header:$2F $F1 $F1 $3 )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescIoCtrlShortTermAdjCPID_F1F1(DescMsgContext* pMsgContext)
{
	if(pMsgContext->reqData[0] < 0xFF)
	{
		if( (uint8)E_OK == CPID_0xF1F1ShortTermAdj(pMsgContext->resData) )
		{}
		/* Received data is in range process further. */
		/* Dummy example of how to write the response data. */
		pMsgContext->resData = pMsgContext->reqData;
		/* Always set the correct length of the response data. */
		pMsgContext->resDataLen = pMsgContext->reqDataLen;
	}
	else
	{
		/* Request contains invalid data - send negative response! */
		DescSetNegResponse(kDescNrcRequestOutOfRange);
	}
	/* User service processing finished. */
	DescProcessingDone();


}


/*  ********************************************************************************
 * Function name:ApplDescIoCtrlRetCtrlToEcuCPID_F1F2 (Service request header:$2F $F1 $F2 $0 )
 * Description:
 * 
 * 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescIoCtrlRetCtrlToEcuCPID_F1F2(DescMsgContext* pMsgContext)
{
  /* <<TBD>> Remove this comment once you have completely implemented this function!!! */
  /* Contains no request data */
  /* Contains no response data */
  /* User service processing finished. */
  DescProcessingDone();
}


/*  ********************************************************************************
 * Function name:ApplDescIoCtrlShortTermAdjCPID_F1F2 (Service request header:$2F $F1 $F2 $3 )
 * Description:
 * 
 * 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescIoCtrlShortTermAdjCPID_F1F2(DescMsgContext* pMsgContext)
{
	if(pMsgContext->reqData[0] < 0xFF)
	{
		if( (uint8)E_OK == CPID_0xF1F2ShortTermAdj(pMsgContext->resData) )
		{}
		/* Received data is in range process further. */
		/* Dummy example of how to write the response data. */
		pMsgContext->resData = pMsgContext->reqData;
		/* Always set the correct length of the response data. */
		pMsgContext->resDataLen = pMsgContext->reqDataLen;
	}
	else
	{
		/* Request contains invalid data - send negative response! */
		DescSetNegResponse(kDescNrcRequestOutOfRange);
	}
	/* User service processing finished. */
	DescProcessingDone();


}


/*  ********************************************************************************
 * Function name:ApplDescIoCtrlRetCtrlToEcuCPID_F1F3 (Service request header:$2F $F1 $F3 $0 )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescIoCtrlRetCtrlToEcuCPID_F1F3(DescMsgContext* pMsgContext)
{
  /* <<TBD>> Remove this comment once you have completely implemented this function!!! */
  /* Contains no request data */
  /* Contains no response data */
  /* User service processing finished. */
  DescProcessingDone();
}


/*  ********************************************************************************
 * Function name:ApplDescIoCtrlShortTermAdjCPID_F1F3 (Service request header:$2F $F1 $F3 $3 )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescIoCtrlShortTermAdjCPID_F1F3(DescMsgContext* pMsgContext)
{
	if(pMsgContext->reqData[0] < 0xFF)
	{
		if( (uint8)E_OK == CPID_0xF1F3ShortTermAdj(pMsgContext->resData) )
		{}
		/* Received data is in range process further. */
		/* Dummy example of how to write the response data. */
		pMsgContext->resData = pMsgContext->reqData;
		/* Always set the correct length of the response data. */
		pMsgContext->resDataLen = pMsgContext->reqDataLen;
	}
	else
	{
		/* Request contains invalid data - send negative response! */
		DescSetNegResponse(kDescNrcRequestOutOfRange);
	}
	/* User service processing finished. */
	DescProcessingDone();


}


/*  ********************************************************************************
 * Function name:ApplDescIoCtrlRetCtrlToEcuCPID_0x2004_Bidet_control (Service request header:$2F $20 $4 $0 )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescIoCtrlRetCtrlToEcuCPID_F1F4(DescMsgContext* pMsgContext)
{
  /* <<TBD>> Remove this comment once you have completely implemented this function!!! */
  /* Contains no request data */
  /* Contains no response data */
  /* User service processing finished. */
  DescProcessingDone();
}


/*  ********************************************************************************
 * Function name:ApplDescIoCtrlShortTermAdjCPID_F1F4 (Service request header:$2F $F1 $F4 $3 )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescIoCtrlShortTermAdjCPID_F1F4(DescMsgContext* pMsgContext)
{
	if(pMsgContext->reqData[0] < 0xFF)
	{
		if( (uint8)E_OK == CPID_0xF1F4ShortTermAdj(pMsgContext->resData) )
		{}
		/* Received data is in range process further. */
		/* Dummy example of how to write the response data. */
		pMsgContext->resData = pMsgContext->reqData;
		/* Always set the correct length of the response data. */
		pMsgContext->resDataLen = pMsgContext->reqDataLen;
	}
	else
	{
		/* Request contains invalid data - send negative response! */
		DescSetNegResponse(kDescNrcRequestOutOfRange);
	}
	/* User service processing finished. */
	DescProcessingDone();


}


/*  ********************************************************************************
 * Function name:ApplDescIoCtrlRetCtrlToEcuCPID_F1F5 (Service request header:$2F $F1 $F5 $0 )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescIoCtrlRetCtrlToEcuCPID_F1F5(DescMsgContext* pMsgContext)
{
  /* <<TBD>> Remove this comment once you have completely implemented this function!!! */
  /* Contains no request data */
  /* Contains no response data */
  /* User service processing finished. */
  DescProcessingDone();
}


/*  ********************************************************************************
 * Function name:ApplDescIoCtrlShortTermAdjCPID_F1F5 (Service request header:$2F $F1 $F5 $3 )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescIoCtrlShortTermAdjCPID_F1F5(DescMsgContext* pMsgContext)
{
	if(pMsgContext->reqData[0] < 0xFF)
	{
		if( (uint8)E_OK == CPID_0xF1F5ShortTermAdj(pMsgContext->resData) )
		{}
		/* Received data is in range process further. */
		/* Dummy example of how to write the response data. */
		pMsgContext->resData = pMsgContext->reqData;
		/* Always set the correct length of the response data. */
		pMsgContext->resDataLen = pMsgContext->reqDataLen;
	}
	else
	{
		/* Request contains invalid data - send negative response! */
		DescSetNegResponse(kDescNrcRequestOutOfRange);
	}
	/* User service processing finished. */
	DescProcessingDone();


}


/*  ********************************************************************************
 * Function name:ApplDescControlDtcSettingEnable (Service request header:$85 $1 )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescControlDtcSettingEnable(DescMsgContext* pMsgContext)
{
  /* <<TBD>> Remove this comment once you have completely implemented this function!!! */
  /* Contains no request data */
  /* Contains no response data */
  /* User service processing finished. */
	uint8 LeApplDesc_u_ErrorCode;
	LeApplDesc_u_ErrorCode = CanDem_DcmEnableDTCSetting();
	if(LeApplDesc_u_ErrorCode == kDescNrcNone)
	{
		/* Received data is in range process further. */
		/* Contains no response data */
	}
	else
	{
		/* Request contains invalid data - send negative response! */
		DescSetNegResponse(LeApplDesc_u_ErrorCode);
	}
	/* User service processing finished. */
	DescProcessingDone();
}


/*  ********************************************************************************
 * Function name:ApplDescControlDtcSettingDisable (Service request header:$85 $2 )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescControlDtcSettingDisable(DescMsgContext* pMsgContext)
{
  /* <<TBD>> Remove this comment once you have completely implemented this function!!! */
  /* Contains no request data */
  /* Contains no response data */
  /* User service processing finished. */
	uint8 LeApplDesc_u_ErrorCode;
	LeApplDesc_u_ErrorCode = CanDem_DcmDisableDTCSetting();
	if(LeApplDesc_u_ErrorCode == kDescNrcNone)
	{
		/* Received data is in range process further. */
		/* Contains no response data */
	}
	else
	{
		/* Request contains invalid data - send negative response! */
		DescSetNegResponse(LeApplDesc_u_ErrorCode);
	}
	/* User service processing finished. */
	DescProcessingDone();
}



///////////////////////////////////////////////////////////////////////////////////////////////////////

/*  ********************************************************************************
 * Function name:ApplDescPreWriteDidDID_0xF1F6 (Service request header:$2E $F1 $F6 )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s): none
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" may not be called (within this pre-handler or
 * later before a main-handler is called).
 *   - The function "DescSetNegResponse" can be called from now on (within this
 * pre-handler or later but before calling "DescProcessingDone").
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescPreWriteDidDID_0xF1F6(void)
{
	if((1 == PendingVehicleSpeed()) &&(1 == PendingPDCM_actualGear()))
	{
		//DescProcessingDone();

	}
	else
	{	
		DescSetNegResponse(0x22); 
	//	DescSessionTransitionChecked(DESC_CONTEXT_PARAM_ONLY);
	}
}




/*  ********************************************************************************
 * Function name:ApplDescPreIoCtrlShortTermAdjCPID_F1F3 (Service request header:$2F $F1 $F3 $3 )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s): none
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" may not be called (within this pre-handler or
 * later before a main-handler is called).
 *   - The function "DescSetNegResponse" can be called from now on (within this
 * pre-handler or later but before calling "DescProcessingDone").
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescPreIoCtrlShortTermAdjCPID_F1F3(void)
{
	if((1 == PendingVehicleSpeed()) &&(1 == PendingPDCM_actualGear()))
	{
	//	DescProcessingDone();

	}
	else
	{	
		DescSetNegResponse(0x22); 
		//	DescSessionTransitionChecked(DESC_CONTEXT_PARAM_ONLY);
	}
}


/*  ********************************************************************************
 * Function name:ApplDescPreControlDtcSettingEnable (Service request header:$85 $1 )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s): none
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" may not be called (within this pre-handler or
 * later before a main-handler is called).
 *   - The function "DescSetNegResponse" can be called from now on (within this
 * pre-handler or later but before calling "DescProcessingDone").
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescPreControlDtcSettingEnable(void)
{
	if((1 == PendingVehicleSpeed()) &&(1 == PendingPDCM_actualGear()))
	{

	}
	else
	{	
		DescSetNegResponse(0x22); 
	}
}

/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/*  ********************************************************************************
 * Function name:ApplDescPostEcuResetHard (Service request header:$11 $1 )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - status:
 *       - Shows the status of the process accomplishment. Can be: kDescPostHandlerStateOk or kDescPostHandlerStateNegResSent or/and kDescPostHandlerStateTxFailed.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" may not be called (within this post-handler or
 * later before a main-handler is called).
 *   - The function "DescSetNegResponse" may not be called (within this post-handler or
 * later before at least a pre-handler is called).
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescPostEcuResetHard(vuint8 status)
{
  /* <<TBD>> Remove this comment once you have completely implemented this function!!! */
    /* Avoid warnings */
  DESC_CONTEXT_PARAM_DUMMY_USE;

  /* Check if this service may be executed (condition checks). This one is dummy check please remove it!. */
  if(status == kDescPostHandlerStateOk)
  {
    /* Do post processing since everything ran without errors (the positive response (process finalization)
     * was successfully sent (closed). 
     */
	  SystemSoftwareReset();
  }
  else
  {
    /* Optional code if the failure reason is from importance. */

    /* The ... */
    if((status & kDescPostHandlerStateNegResSent) != 0)
    {
      /* ... negative response ... */
    }
    else
    {
      /* ... positive response ... */
    }

    if((status & kDescPostHandlerStateTxFailed) != 0)
    {
      /* ... transmission has failed. */
    }
    else
    {
      /* ... transmission has been successfully accomplished. */
    }
  }
;
}


/*  ********************************************************************************
 * Function name:ApplDescPostEcuResetSoft (Service request header:$11 $3 )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - status:
 *       - Shows the status of the process accomplishment. Can be: kDescPostHandlerStateOk or kDescPostHandlerStateNegResSent or/and kDescPostHandlerStateTxFailed.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" may not be called (within this post-handler or
 * later before a main-handler is called).
 *   - The function "DescSetNegResponse" may not be called (within this post-handler or
 * later before at least a pre-handler is called).
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescPostEcuResetSoft(vuint8 status)
{
  /* <<TBD>> Remove this comment once you have completely implemented this function!!! */
    /* Avoid warnings */
  DESC_CONTEXT_PARAM_DUMMY_USE;

  /* Check if this service may be executed (condition checks). This one is dummy check please remove it!. */
  if(status == kDescPostHandlerStateOk)
  {
    /* Do post processing since everything ran without errors (the positive response (process finalization)
     * was successfully sent (closed). 
     */
	  SystemSoftwareReset();
  }
  else
  {
    /* Optional code if the failure reason is from importance. */

    /* The ... */
    if((status & kDescPostHandlerStateNegResSent) != 0)
    {
      /* ... negative response ... */
    }
    else
    {
      /* ... positive response ... */
    }

    if((status & kDescPostHandlerStateTxFailed) != 0)
    {
      /* ... transmission has failed. */
    }
    else
    {
      /* ... transmission has been successfully accomplished. */
    }
  }
;
}


/* ********************************************************************************
 * Function name:ApplDescOnCommunicationDisable
 * Description: Notification function that the communication has been disabled.
 * Returns:  none
 * Parameter(s):none
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" may not be called.
 *   - The function "DescSetNegResponse" may not be called.
 ******************************************************************************** */
void DESC_API_CALLBACK_TYPE ApplDescOnCommunicationDisable(void)
{
  /*<<TBD>> Remove this comment once you have completely implemented this function!!!*/
  /* The requested group of messages will no more be sent onto the communication bus. */
}

/* ********************************************************************************
 * Function name:ApplDescOnCommunicationEnable
 * Description: Notification function that the communication has been restored.
 * Returns:  none
 * Parameter(s):none
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" may not be called.
 *   - The function "DescSetNegResponse" may not be called.
 ******************************************************************************** */
void DESC_API_CALLBACK_TYPE ApplDescOnCommunicationEnable(void)
{
  /*<<TBD>> Remove this comment once you have completely implemented this function!!!*/
  /* The requested group of messages will be able to be sent onto the communication bus. */
}

/* Check the communication control activity */
/* ********************************************************************************
 * Function name:ApplDescCheckCommCtrl
 * Description:Check if the requested communication manipulation is possible to be performed by the ECU.
 * Returns:  nothing
 * Parameter(s):
 *   - iContext:
 *       - Diagnostic request handle used only in multi-context system (kDescNumContexts > 1).
 *       - Access type: read
 *   - commControlInfo->subNetTxNumber:
 *       - The application shall use this parameter to decied on which physical channels the communiaction will be manipulated.
 *       - Access type: read
 *   - commControlInfo->commCtrlChannel:
 *       - The application determines on which channel the communication will be manipulated (kDescCommControlCanCh01 - kDescCommControlCanCh14).
 *       - Access type: write
 *   - commControlInfo->rxPathState:
 *       - Activity type: kDescCommControlStateEnable - enables the communication; kDescCommControlStateDisable - disables it.
 *       - Access type: read
 *   - commControlInfo->txPathState:
 *       - Activity type: kDescCommControlStateEnable - enables the communication; kDescCommControlStateDisable - disables it.
 *       - Access type: read
 *   - commControlInfo->msgTypes:
 *       - Message group: kDescCommControlMsgAppl - application; kDescCommControlMsgNm - NM; 
 *       - Access type: read
 *   - commControlInfo->reqCommChannel:
 *       - The current CAN channel on which the CommControl request is received.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - Call "DescCommCtrlChecked" (here or later) to confirm the check.
 *   - Call "DescSetNegResponse" to reject the service.
 ******************************************************************************** */
void DESC_API_CALLBACK_TYPE ApplDescCheckCommCtrl(DESC_CONTEXT_FORMAL_PARAM_DEF_FIRST DescOemCommControlInfo *commControlInfo)
{
  /*<<TBD>> Remove this comment once you have completely implemented this function!!!*/

#if defined (DESC_ENABLE_COMM_CTRL_SUBNET_SUPPORT)
  switch(commControlInfo->subNetNumber)
  {
    case kDescCommControlSubNetNumAll:/* Enable/Disable all comm channels (CAN, LIN, etc.)*/
	{
		
		if( (commControlInfo->rxPathState == (DescBitType)0x01U) && (commControlInfo->txPathState == (DescBitType)0x01U) )   //28 00
		{
			if( commControlInfo->msgTypes == (DescBitType)0x01U )  /*App msg, enable Tx and Rx*/  
			{
				IlTxStart();
				IlRxStart();
				AppDesc_28Service_Ctrl_Flag = 0u;
			}
			else if(commControlInfo->msgTypes == (DescBitType)0x03U) /*NM AND APP Msg ,enable Tx and Rx*/
			{
				(void)Nm_EnableCommunication(0u);
				IlTxStart();
				IlRxStart();
				AppDesc_28Service_Ctrl_Flag = 0u;			
			}
			else if(commControlInfo->msgTypes == (DescBitType)0x02U)/*cg*/
			{
				(void)Nm_EnableCommunication(0u);
				AppDesc_28Service_Ctrl_Flag = 0u;
			}
			else
			{
				DescSetNegResponse(DESC_CONTEXT_PARAM_FIRST kDescNrcRequestOutOfRange);
			}
		}
		else if( (commControlInfo->rxPathState == (DescBitType)0x00U) && (commControlInfo->txPathState == (DescBitType)0x00U) )  //28 03
		{
			if( commControlInfo->msgTypes == (DescBitType)0x01U) /*App msg, disable Tx and Rx*/
			{
				IlTxStop();
				IlRxStop();
				AppDesc_28Service_Ctrl_Flag = 1u;
			}
			else if(commControlInfo->msgTypes == (DescBitType)0x03U) /*NM AND APP Msg ,disable Tx and Rx*/
			{
				(void)Nm_DisableCommunication(0u);
				IlTxStop();
				IlRxStop();
				AppDesc_28Service_Ctrl_Flag = 1u;
			}
			else if(commControlInfo->msgTypes == (DescBitType)0x02U)/*cg*/
			{
				(void)Nm_DisableCommunication(0u);
			//	AppDesc_28Service_Ctrl_Flag = 1u;
        AppDesc_28Service_CtrlNM_Flag = 1u;
			}
			else
			{
				DescSetNegResponse(DESC_CONTEXT_PARAM_FIRST kDescNrcRequestOutOfRange);
			}
		}
		else if( (commControlInfo->rxPathState == (DescBitType)0x01U) && (commControlInfo->txPathState == (DescBitType)0x00U) )  //28 01
		{
			if( commControlInfo->msgTypes == (DescBitType)0x01U) /*App msg, Enable Rx And Disable Tx*/
			{
				IlTxStop();
				IlRxStart();
				AppDesc_28Service_Ctrl_Flag = 1u;
			}
			else if(commControlInfo->msgTypes == (DescBitType)0x03U) /*NM AND APP Msg ,Enable Rx And Disable Tx*/
			{
				(void)Nm_DisableCommunication(0u);
				IlTxStop();
				IlRxStart();
				AppDesc_28Service_Ctrl_Flag = 1u;
			}
			else if(commControlInfo->msgTypes == (DescBitType)0x02U)/*cg*/
			{
				(void)Nm_DisableCommunication(0u);
				AppDesc_28Service_Ctrl_Flag = 1u;
			}
			else
			{
				DescSetNegResponse(DESC_CONTEXT_PARAM_FIRST kDescNrcRequestOutOfRange);
			}
		}
		else  //28 02
		{
			if( commControlInfo->msgTypes == (DescBitType)0x01U) /*App msg, Disable Rx And Enable Tx*/
			{
				IlRxStop();
				IlTxStart();
				AppDesc_28Service_Ctrl_Flag = 1u;
			}
			else if(commControlInfo->msgTypes == (DescBitType)0x03U) /*NM AND APP Msg ,Disable Rx And Enable Tx*/
			{
				(void)Nm_EnableCommunication(0u);
				IlRxStop();
				IlTxStart();
				AppDesc_28Service_Ctrl_Flag = 1u;
			}
			else if(commControlInfo->msgTypes == (DescBitType)0x02U)/*cg*/
			{
				(void)Nm_EnableCommunication(0u);
				AppDesc_28Service_Ctrl_Flag = 1u;
			}
			else
			{
				DescSetNegResponse(DESC_CONTEXT_PARAM_FIRST kDescNrcRequestOutOfRange);
			}
		}
	}
      break;
    case kDescCommControlSubNetNumRx:/* Enable/Disable only the request RX comm channel (CAN)*/
      /* Nothing to do - CANdesc handles the CAN channels */
      break;
    default:
      /* Dispatch subnetworks */
      /* Assumed subnet to CAN mapping: 
       * SubNet 1: LIN 
       * SubNet 2: CAN1 
       * SubNet 3: CAN2 
       */
      switch(commControlInfo->subNetNumber)
      {
        case kDescCommControlSubNetNum01:
          /* LIN network */
          commControlInfo->commCtrlChannel = kDescCommControlCanChNone;
          break;
/* The part is only needed if a multi CAN system is designed !!! */
# if defined(DESC_ENABLE_MULTI_CHANNEL_SUPPORT)
        case kDescCommControlSubNetNum02:
          /* CAN1 */
          commControlInfo->commCtrlChannel = kDescCommControlCanCh01;
          break;
        case kDescCommControlSubNetNum03:
          /* CAN2 */
          commControlInfo->commCtrlChannel = kDescCommControlCanCh02;
          break;
# endif
        default:
          /* Subnet numbers out of range */
          DescSetNegResponse(DESC_CONTEXT_PARAM_FIRST kDescNrcRequestOutOfRange);
          break;
      }
  }
#endif
  /* Confirm the communication status will be accepted. 
   * This can be done later by storing the iContext parameter (if exists) globaly to be able to give the correct acknowledgment.
   */
  DescCommCtrlChecked(DESC_CONTEXT_PARAM_ONLY);
}

#if defined (DESC_ENABLE_COMM_CTRL_SUBNET_SUPPORT)
/* ********************************************************************************
 * Function name:ApplDescSetCommMode
 * Description:Manipulate application specific channels (LIN, MOST, etc.)
 * Returns:  nothing
 * Parameter(s):
 *   - commControlInfo->subNetTxNumber:
 *       - The application shall use this parameter to decied on which physical channels the communiaction will be manipulated.
 *       - Access type: read
 *   - commControlInfo->commCtrlChannel:
 *       - The application determines on which channel the communication will be manipulated (kDescCommControlCanCh01 - kDescCommControlCanCh14).
 *       - Access type: write
 *   - commControlInfo->rxPathState:
 *       - Activity type: kDescCommControlStateEnable - enables the communication; kDescCommControlStateDisable - disables it.
 *       - Access type: read
 *   - commControlInfo->txPathState:
 *       - Activity type: kDescCommControlStateEnable - enables the communication; kDescCommControlStateDisable - disables it.
 *       - Access type: read
 *   - commControlInfo->msgTypes:
 *       - Message group: kDescCommControlMsgAppl - application; kDescCommControlMsgNm - NM; 
 *       - Access type: read
 *   - commControlInfo->reqCommChannel:
 *       - The current CAN channel on which the CommControl request is received.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" may not be called.
 *   - The function "DescSetNegResponse" may not be called.
 ******************************************************************************** */
void DESC_API_CALLBACK_TYPE ApplDescSetCommMode(DescOemCommControlInfo *commControlInfo)
{
  switch(commControlInfo->subNetNumber)
  {
    case kDescCommControlSubNetNumAll:/* Enable/Disable all comm channels (CAN, LIN, etc.)*/
      /* !!! Process application specific channels (e.g. LIN) CAN are already enabled/disabled by CANdesc */
      break;
    case kDescCommControlSubNetNumRx:/* Enable/Disable only the request RX comm channel (CAN)*/
      /* Nothing to do - CANdesc handles the CAN channels */
      break;
    default:
      /* Dispatch subnetworks */
      /* Assumed subnet to CAN mapping: 
       * SubNet 1: LIN 
       * SubNet 2: CAN1 
       * SubNet 3: CAN2 
       */
      switch(commControlInfo->subNetNumber)
      {
        case kDescCommControlSubNetNum01:
          /* LIN network */
          /* !!! Process application specific channels (e.g. LIN)*/
          break;
        case kDescCommControlSubNetNum02:          /* CAN1 */
        case kDescCommControlSubNetNum03:          /* CAN2 */
          /* Nothing to do - CANdesc handles all CANs */
          break;
        default:break;
      }
  }
}
#endif

#if defined (DESC_ENABLE_RX_COMM_CONTROL)
/* ********************************************************************************
 * Function name:ApplDescSetCommModeOnRxPath
 * Description: Manipulates only the RX path on CAN. For the other networks (if any) such LIN, MOST, etc. reffer to the
 *              ApplDescSetCommMode API.
 * Returns:  nothing
 * Parameter(s):
 *   - commControlInfo->subNetTxNumber:
 *       - The application shall use this parameter to decied on which physical channels the communiaction will be manipulated.
 *       - Access type: read
 *   - commControlInfo->commCtrlChannel:
 *       - The application determines on which channel the communication will be manipulated (kDescCommControlCanCh01 - kDescCommControlCanCh14).
 *       - Access type: write
 *   - commControlInfo->rxPathState:
 *       - Activity type: kDescCommControlStateEnable - enables the communication; kDescCommControlStateDisable - disables it.
 *       - Access type: read
*   - commControlInfo->rxPathState: - irrelevant for this API since it processes only the RX path!!!
 *       - Activity type: kDescCommControlStateEnable - enables the communication; kDescCommControlStateDisable - disables it.
 *       - Access type: read
 *   - commControlInfo->msgTypes:
 *       - Message group: kDescCommControlMsgAppl - application; kDescCommControlMsgNm - NM; 
 *       - Access type: read
 *   - commControlInfo->reqCommChannel:
 *       - The current CAN channel on which the CommControl request is received.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" may not be called.
 *   - The function "DescSetNegResponse" may not be called.
 ******************************************************************************** */
void DESC_API_CALLBACK_TYPE ApplDescSetCommModeOnRxPath(DescOemCommControlInfo *commControlInfo)
{
  if((commControlInfo->rxPathState & kDescCommControlStateEnable) != 0)
  {
    /* _DrvCanSetRxOnlineMode(commControlInfo.commCtrlChannel, g_descCommControlInfo.msgTypes); */
  }
  else
  {
    /* _DrvCanSetRxOfflineMode(commControlInfo.commCtrlChannel, g_descCommControlInfo.msgTypes); */
  }
}
#endif

/*  ********************************************************************************
 * Function name:ApplDescCheckSessionTransition
 * Description:Check if the given session transition is allowed or your ECU is currently not
 * able to perform it.
 * Returns:  nothing
 * Parameter(s):
 *   - newState:
 *       - Contains the new state in which the state group will be set.
 *       - Access type: read
 *   - formerState:
 *       - Contains the current state of the state group.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescSessionTransitionChecked" may be called.
 *   - The function "DescSetNegResponse" may be called.
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescCheckSessionTransition(DescStateGroup newState, DescStateGroup formerState)
{
	uint16 e_u_ADValue;
	
	if(newState == kDescStateSessionDefault)
	{
		if(g_descSecureNrOfAttempts == 3)
		{
			g_descSecureTimer  = kDescPowerOnSecureTimer /10U;
		}
		
	}
	
    if (newState == kDescStateSessionProgramming)
    {
		
		e_u_ADValue = ADCM_GetValidValue((uint8)ADCMCFG_KL30_AD_CH);
		if((e_u_ADValue >= BTRCTR_SYS_BATTERY_B2A_UP_VOLT) && (e_u_ADValue <= BTRCTR_SYS_BATTERY_B2A_DOWN_VOLT))/*8.5-16v programming*/
		{
			
			
			if((1 == PendingVehicleSpeed()) &&(1 == PendingPDCM_actualGear()))
			{
				reprogrammingRequest = kDescProgramming_Flag;
				DescForceRcrRpResponse();

			}
			else
			{
				DescSetNegResponse(0x22); 
				DescSessionTransitionChecked(DESC_CONTEXT_PARAM_ONLY);
			}
	
			reprogrammingRequest = kDescProgramming_Flag;
			DescForceRcrRpResponse();
		}
		else
		{
		  DescSetNegResponse(( DescNegResCode )kDescNrcConditionsNotCorrect);
		  DescProcessingDone();
		}         

    }
    else
    {
        DescSessionTransitionChecked(DESC_CONTEXT_PARAM_ONLY);
    }
}


#if defined (DESC_ENABLE_REPORT_ATT_CNTR_STATE)
/* ********************************************************************************
 * Function name:ApplDescSecurityAccessOnAttCntrChanged
 * Description: Once the invalid key attempt counter has been changed, this event is triggered.
 * Returns:  Random value
 * Parameter(s):
 *   - iContext:(not available in single context systems)
 *       - Use this call-back handle for all API which need it.
 *       - Access type: read
 *   - chgEv: specifies the reason for hte counter change
 *       - You can use this information in order to perform special activities if needed.
 *       - Access type: read
 *   - newAttCounter: new attempt counter value
 *       - If required, you can store this value into NVM.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" is NOT allowed to be called.
 *   - The function "DescSetNegResponse" is NOT allowed to be called.
 ******************************************************************************** */
void ApplDescSecurityAccessOnAttCntrChanged(DESC_CONTEXT_FORMAL_PARAM_DEF_FIRST DescSecAccAttCtrChgEvType chgEv, vuint8 newAttCounter)
{
  switch(chgEv)
  {
    case kDescSecAccAttCtrChgOnTimeExpire:
      /* Event that the penalty time expired - do something if needed */
      break;
    case kDescSecAccAttCtrChgOnTooManyInvalidKeys:
      /* Event that too many invalid keys were received - do something if needed */
    case kDescSecAccAttCtrChgOnInvalidKey: /* fall through */
      /* Event that an invalid key received - do something if needed */
      break;
# if defined (DESC_ENABLE_ATT_CNTR_RESET_ON_KEY_OK)
    case kDescSecAccAttCtrChgOnValidKey:
      /* Event that a valid key is received - do something if needed */
      break;
# endif
# if defined(DESC_ENABLE_USE_ERR_CNTR_PROTECTION)
    case kDescSecAccAttCtrChgOnTooManySeedsWithoutKey:
      /* Event that too many seed requests without a send key were received - do something if needed */
    case kDescSecAccAttCtrChgOnSeedWithoutKey: /* fall through */
      /* Event that another seed request without a send key is received - do something if needed */
      break;
# endif
    case kDescSecAccAttCtrChgOnApplRequest:
      /* Event that "DescSetAttemptCounterValue" has changed the counter value.
       * Usually you shall ignore this event type, since it is triggered by the application, but
       * to have complete "OnChange" chain this event will be triggered.*/
      return; /* usually you shall skip updating NVM */
    case kDescSecAccAttCtrChgInvalid:
      /* Invalid event type shall never occur!!! */
    default: /* fall through */
      /* not a valid case - do assert */
      return;/* do not store anything */
  }

  /*
  Optionally you can still access the current (not yet changed) value of the counter */
  if ( (DescGetAttemptCounterValue(DESC_CONTEXT_PARAM_ONLY) == 0) &&
       (newAttCounter == 1) )
  {
    /* do something on very first increment of the counter */
  }


  /* Update the attempt counter in the NVM in all cases
  eepWrite(secAccessAttCntrHndl, newAttCounter);
  */
}
#endif

/******************************************************************************
* Name         :  SecM_ComputeKey  
* Called by    :  CheckKey
* Preconditions:  None
* Parameters   :  None
* Description  :  
******************************************************************************/
static vuint32 SecM_ComputeKey(void)
{
    uint32 KeyResult;
    
    KeyResult = ((((g_applDescSeedX >> 1) ^ g_applDescSeedX) <<3) ^ (g_applDescSeedX >>2));
	return (uint32)(KeyResult ^ 0X01C5U); 

}

/******************************************************************************
* Name         :  SecM_Serializer  
* Called by    :  CheckKey
* Preconditions:  None
* Parameters   :  None
* Description  :  
******************************************************************************/
static void SecM_Serializer(DescMsg tgtPtr, vuint32 data)
{
  vuint8_least iter;
  iter = 4;
  while(iter != 0)
  {
    iter--;
    tgtPtr[iter] = (DescMsgItem)data;
    data >>= 8;
  }
}

/* ********************************************************************************
 * Function name:ApplDescSecurityAccessGetSeed
 * Description: Each time called must generate a different value (e.g. current free running timer value).
 * Returns:  Random value
 * Parameter(s):
 *   - iContext:(not available in single context systems)
 *       - Use this call-back handle for all API which need it.
 *       - Access type: read
 *   - securityLevel:
 *       - The current security level represented by the generated contsants (e.g kDescStateSecurityAccessLevel1e etc.).
 *       - Access type: read
 *   - seed:
 *       - A pointer where the generated seed must be written by the application.
 *       - Access type: write
 *   - seedLen:
 *       - The length of the written seed.
 *       - Access type: write
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" is NOT allowed to be called.
 *   - The function "DescSetNegResponse" may be called.
 ******************************************************************************** */
void ApplDescSecurityAccessGetSeed(DescSecurityAccessContext* pDescSecurityAccessContext)
{
  uint32_t trngvalue[8] = {0};
  uint8_t Req_2701_Seed[4] = {0};
  
   g_applDescSeedX = Random_GetRandomValue();     // ggl  2025/09/26
  ///  EEPM_AppReadBlock(EEP_BLOCK_SEED_ID, &Req_2701_Seed[0] );
 //   g_applDescSeedX = (uint32_t)(Req_2701_Seed[0] | ((Req_2701_Seed[1]<<8)&0xFF00) | ((Req_2701_Seed[2]<<16)&0xFF0000)  | ((Req_2701_Seed[3]<<24)&0xFF000000));
  

    if((g_applDescSeedX == 0x00000000u) || (g_applDescSeedX == 0xFFFFFFFFu))
    {
        g_applDescSeedX = 0x00000002u;
    }


    /*32Bit*/
    SecM_Serializer(&(pDescSecurityAccessContext->dataPtr[0]), g_applDescSeedX);
    
    
    /* dataLen parameter is already initialized with the seed length from the CDD file */
    /* pDescSecurityAccessContext->dataLen = 2;*/
    
    /* Call this function to confirm the process end now or outside of this call-back */
    DescSecurityAccessSeedReady(DESC_CONTEXT_PARAM_WRAPPER_ONLY(pDescSecurityAccessContext->iContext));
}

/* ********************************************************************************
 * Function name:ApplDescSecurityAccessCheckKey
 * Description: The application must validate the received security key.
 * Returns:  Random value
 * Parameter(s):
 *   - iContext:(not available in single context systems)
 *       - Use this call-back handle for all API which need it.
 *       - Access type: read
 *   - securityLevel:
 *       - The current security level represented by the generated contsants (e.g kDescStateSecurityAccessLevel1e etc.).
 *       - Access type: read
 *   - key:
 *       - A pointer where the received key must be read from by the application.
 *       - Access type: read
 *   - keyLength:
 *       - The length of the received key.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" is NOT allowed be called.
 *   - The function "DescSetNegResponse" may be called.
 ******************************************************************************** */
//uint32	Temp_CVAL1;
//uint32	Temp_CVAL2;
void ApplDescSecurityAccessCheckKey(DescSecurityAccessContext* pDescSecurityAccessContext)
{
   /* The size of this array may vary and depends on the security key definition in the CDD file. */
      /* Store Key on the stack, invisible for extern access */
  
  vuint8  applDescSeed[4];
  

 // pTMR_DRV_InitChannel(0, pTMR0_CH0, &ptmr_channel_0);

  // pTMR_Type * base;

   // base = 0x4005A000U;//s_ptmrBase[0];
    
  //  base->CH[0].TCV = 0x00000000u;
  
  /* Key is OK */
  pDescSecurityAccessContext->status = kDescSaStatusOk;

  
 // Temp_CVAL1 = base->CH[0].TCV;
  /* Calculate the Key */
  tmpKey = CanApp_SecurityAccessCheckKey(pDescSecurityAccessContext->securityLevel);

  /* Compare results with Key from tester */
  if (tmpKey != DescMake32Bit(pDescSecurityAccessContext->dataPtr[0],
                              pDescSecurityAccessContext->dataPtr[1],
                              pDescSecurityAccessContext->dataPtr[2],
                              pDescSecurityAccessContext->dataPtr[3])
      )
  {
    /* Key is INVALID */
    pDescSecurityAccessContext->status = kDescSaStatusNok; 
	  
  }
  else
  {
	/*  g_applDescSeedX = Random_GetRandomValue();
	  
	  
	  applDescSeed[0] = (vuint8)(g_applDescSeedX & 0xFF);
	  applDescSeed[1] =	(vuint8)((g_applDescSeedX & 0xFF00) >> 8);
	  applDescSeed[2] = (vuint8)((g_applDescSeedX & 0xFF0000) >> 16);
	  applDescSeed[3] = (vuint8)((g_applDescSeedX & 0xFF000000) >> 24);*/
	  
	  
	  
	 // EEPM_AppWriteBlock(EEP_BLOCK_SEED_ID, &applDescSeed[0]);
	  DescSetAttemptCounterValue(0);
	  tmpKey = 0x00;
	 // Temp_CVAL2 = base->CH[0].TCV;
  }
  /* Call this function to confirm the process end now or outside of this call-back */
  DescSecurityAccessKeyChecked(DESC_CONTEXT_PARAM_WRAPPER_ONLY(pDescSecurityAccessContext->iContext));
}

/*  ********************************************************************************
 * Function name:ApplDescReadDidDID_0xF089 (Service request header:$22 $F0 $89 )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescReadDidDID_0xF089(DescMsgContext* pMsgContext)
{
	vuint8 LeApplDesc_u_RetCode;

	LeApplDesc_u_RetCode = DID_ReadDid0xF089( pMsgContext->resData );		
	if( kDescNrcNone == LeApplDesc_u_RetCode )
	{
		pMsgContext->resDataLen = 15;
	}
	else
	{	  
		DescSetNegResponse( (DescNegResCode )LeApplDesc_u_RetCode ); 
	} 
	DescProcessingDone();
}


/*  ********************************************************************************
 * Function name:ApplDescReadDidDID_0xF120 (Service request header:$22 $F1 $20 )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescReadDidDID_0xF120(DescMsgContext* pMsgContext)
{
	vuint8 LeApplDesc_u_RetCode;

	LeApplDesc_u_RetCode = DID_ReadDid0xF120( pMsgContext->resData );		
	if( kDescNrcNone == LeApplDesc_u_RetCode )
	{
		pMsgContext->resDataLen = 4;
	}
	else
	{	  
		DescSetNegResponse( (DescNegResCode )LeApplDesc_u_RetCode ); 
	} 
	DescProcessingDone();
}


/*  ********************************************************************************
 * Function name:ApplDescReadDidDID_0xF121 (Service request header:$22 $F1 $21 )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescReadDidDID_0xF121(DescMsgContext* pMsgContext)
{
	vuint8 LeApplDesc_u_RetCode;

	LeApplDesc_u_RetCode = DID_ReadDid0xF121( pMsgContext->resData );		
	if( kDescNrcNone == LeApplDesc_u_RetCode )
	{
		pMsgContext->resDataLen = 4;
	}
	else
	{	  
		DescSetNegResponse( (DescNegResCode )LeApplDesc_u_RetCode ); 
	} 
	DescProcessingDone();
}


/*  ********************************************************************************
 * Function name:ApplDescReadDidDID_0xF122 (Service request header:$22 $F1 $22 )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescReadDidDID_0xF122(DescMsgContext* pMsgContext)
{
	vuint8 LeApplDesc_u_RetCode;

	LeApplDesc_u_RetCode = DID_ReadDid0xF122( pMsgContext->resData );		
	if( kDescNrcNone == LeApplDesc_u_RetCode )
	{
		pMsgContext->resDataLen = 4;
	}
	else
	{	  
		DescSetNegResponse( (DescNegResCode )LeApplDesc_u_RetCode ); 
	} 
	DescProcessingDone();
}


/*  ********************************************************************************
 * Function name:ApplDescReadDidDID_0xF170 (Service request header:$22 $F1 $70 )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescReadDidDID_0xF170(DescMsgContext* pMsgContext)
{
	vuint8 LeApplDesc_u_RetCode;

	LeApplDesc_u_RetCode = DID_ReadDid0xF170( pMsgContext->resData );		
	if( kDescNrcNone == LeApplDesc_u_RetCode )
	{
		pMsgContext->resDataLen = 4;
	}
	else
	{	  
		DescSetNegResponse( (DescNegResCode )LeApplDesc_u_RetCode ); 
	} 
	DescProcessingDone();
}


/*  ********************************************************************************
 * Function name:ApplDescReadDidDID_0xF171 (Service request header:$22 $F1 $71 )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescReadDidDID_0xF171(DescMsgContext* pMsgContext)
{
	vuint8 LeApplDesc_u_RetCode;

	LeApplDesc_u_RetCode = DID_ReadDid0xF171( pMsgContext->resData );		
	if( kDescNrcNone == LeApplDesc_u_RetCode )
	{
		pMsgContext->resDataLen = 4;
	}
	else
	{	  
		DescSetNegResponse( (DescNegResCode )LeApplDesc_u_RetCode ); 
	} 
	DescProcessingDone();
}


/*  ********************************************************************************
 * Function name:ApplDescReadDidDID_0xF184 (Service request header:$22 $F1 $84 )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescReadDidDID_0xF184(DescMsgContext* pMsgContext)
{
	vuint8 LeApplDesc_u_RetCode;

	LeApplDesc_u_RetCode = DID_ReadDid0xF184( pMsgContext->resData );		
	if( kDescNrcNone == LeApplDesc_u_RetCode )
	{
		pMsgContext->resDataLen = 7;
	}
	else
	{	  
		DescSetNegResponse( (DescNegResCode )LeApplDesc_u_RetCode ); 
	} 
	DescProcessingDone();
}


/*  ********************************************************************************
 * Function name:ApplDescReadDidDID_0xF187 (Service request header:$22 $F1 $87 )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescReadDidDID_0xF187(DescMsgContext* pMsgContext)
{
	vuint8 LeApplDesc_u_RetCode;

	LeApplDesc_u_RetCode = DID_ReadDid0xF187( pMsgContext->resData );		
	if( kDescNrcNone == LeApplDesc_u_RetCode )
	{
		pMsgContext->resDataLen = 21;
	}
	else
	{	  
		DescSetNegResponse( (DescNegResCode )LeApplDesc_u_RetCode ); 
	} 
	DescProcessingDone();
}


/*  ********************************************************************************
 * Function name:ApplDescReadDidDID_0xF188 (Service request header:$22 $F1 $88 )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescReadDidDID_0xF188(DescMsgContext* pMsgContext)
{
	vuint8 LeApplDesc_u_RetCode;

	LeApplDesc_u_RetCode = DID_ReadDid0xF188( pMsgContext->resData );		
	if( kDescNrcNone == LeApplDesc_u_RetCode )
	{
		pMsgContext->resDataLen = 20;
	}
	else
	{	  
		DescSetNegResponse( (DescNegResCode )LeApplDesc_u_RetCode ); 
	} 
	DescProcessingDone();
}


/*  ********************************************************************************
 * Function name:ApplDescReadDidDID_0xF189 (Service request header:$22 $F1 $89 )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescReadDidDID_0xF189(DescMsgContext* pMsgContext)
{
	vuint8 LeApplDesc_u_RetCode;

	LeApplDesc_u_RetCode = DID_ReadDid0xF189( pMsgContext->resData );		
	if( kDescNrcNone == LeApplDesc_u_RetCode )
	{
		pMsgContext->resDataLen = 15;
	}
	else
	{	  
		DescSetNegResponse( (DescNegResCode )LeApplDesc_u_RetCode ); 
	} 
	DescProcessingDone();
}


/*  ********************************************************************************
 * Function name:ApplDescReadDidDID_0xF18A (Service request header:$22 $F1 $8A )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescReadDidDID_0xF18A(DescMsgContext* pMsgContext)
{
	vuint8 LeApplDesc_u_RetCode;

	LeApplDesc_u_RetCode = DID_ReadDid0xF18A( pMsgContext->resData );		
	if( kDescNrcNone == LeApplDesc_u_RetCode )
	{
		pMsgContext->resDataLen = 7;
	}
	else
	{	  
		DescSetNegResponse( (DescNegResCode )LeApplDesc_u_RetCode ); 
	} 
	DescProcessingDone();
}


/*  ********************************************************************************
 * Function name:ApplDescReadDidDID_0xF18C (Service request header:$22 $F1 $8C )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescReadDidDID_0xF18C(DescMsgContext* pMsgContext)
{
	vuint8 LeApplDesc_u_RetCode;

	LeApplDesc_u_RetCode = DID_ReadDid0xF18C( pMsgContext->resData );		
	if( kDescNrcNone == LeApplDesc_u_RetCode )
	{
		pMsgContext->resDataLen = 13;
	}
	else
	{	  
		DescSetNegResponse( (DescNegResCode )LeApplDesc_u_RetCode ); 
	} 
	DescProcessingDone();
}


/*  ********************************************************************************
 * Function name:ApplDescReadDidDID_0xF1F0 (Service request header:$22 $F1 $F0 )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescReadDidDID_0xF1F0(DescMsgContext* pMsgContext)
{
	vuint8 LeApplDesc_u_RetCode;

	LeApplDesc_u_RetCode = DID_ReadDid0xF1F0( pMsgContext->resData );		
	if( kDescNrcNone == LeApplDesc_u_RetCode )
	{
		pMsgContext->resDataLen = 2;
	}
	else
	{	  
		DescSetNegResponse( (DescNegResCode )LeApplDesc_u_RetCode ); 
	} 
	DescProcessingDone();
}

/*  ********************************************************************************
 * Function name:ApplDescReadDidCPID_F1F1 (Service request header:$22 $F1 $F1 )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescReadDidCPID_F1F1(DescMsgContext* pMsgContext)
{
  /* <<TBD>> Remove this comment once you have completely implemented this function!!! */
  /* Contains no request data */
  /* Dummy example of how to write the response data. */
  pMsgContext->resData[0] = 0xFF;
  /* Always set the correct length of the response data. */
  pMsgContext->resDataLen = 3;
  /* User service processing finished. */
  DescProcessingDone();
}


/*  ********************************************************************************
 * Function name:ApplDescReadDidCPID_F1F2 (Service request header:$22 $F1 $F2 )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescReadDidCPID_F1F2(DescMsgContext* pMsgContext)
{
  /* <<TBD>> Remove this comment once you have completely implemented this function!!! */
  /* Contains no request data */
  /* Dummy example of how to write the response data. */
  pMsgContext->resData[0] = 0xFF;
  /* Always set the correct length of the response data. */
  pMsgContext->resDataLen = 9;
  /* User service processing finished. */
  DescProcessingDone();
}


/*  ********************************************************************************
 * Function name:ApplDescReadDidCPID_F1F3 (Service request header:$22 $F1 $F3 )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescReadDidCPID_F1F3(DescMsgContext* pMsgContext)
{
  /* <<TBD>> Remove this comment once you have completely implemented this function!!! */
  /* Contains no request data */
  /* Dummy example of how to write the response data. */
  pMsgContext->resData[0] = 0xFF;
  /* Always set the correct length of the response data. */
  pMsgContext->resDataLen = 6;
  /* User service processing finished. */
  DescProcessingDone();
}


/*  ********************************************************************************
 * Function name:ApplDescReadDidCPID_F1F4 (Service request header:$22 $F1 $F4 )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescReadDidCPID_F1F4(DescMsgContext* pMsgContext)
{
  /* <<TBD>> Remove this comment once you have completely implemented this function!!! */
  /* Contains no request data */
  /* Dummy example of how to write the response data. */
  pMsgContext->resData[0] = 0xFF;
  /* Always set the correct length of the response data. */
  pMsgContext->resDataLen = 5;
  /* User service processing finished. */
  DescProcessingDone();
}


/*  ********************************************************************************
 * Function name:ApplDescReadDidCPID_F1F5 (Service request header:$22 $F1 $F5 )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescReadDidCPID_F1F5(DescMsgContext* pMsgContext)
{
  /* <<TBD>> Remove this comment once you have completely implemented this function!!! */
  /* Contains no request data */
  /* Dummy example of how to write the response data. */
  pMsgContext->resData[0] = 0xFF;
  /* Always set the correct length of the response data. */
  pMsgContext->resDataLen = 6;
  /* User service processing finished. */
  DescProcessingDone();
}


/*  ********************************************************************************
 * Function name:ApplDescReadDidDID_0xF1F6 (Service request header:$22 $F1 $F6 )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */

/*  ********************************************************************************
 * Function name:ApplDescReadDidDID_0xF1F6 (Service request header:$22 $F1 $F6 )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescReadDidDID_0xF1F6(DescMsgContext* pMsgContext)
{
	vuint8 LeApplDesc_u_RetCode;

	LeApplDesc_u_RetCode = DID_ReadDid0xF1F6( pMsgContext->resData );		
	if( kDescNrcNone == LeApplDesc_u_RetCode )
	{
		pMsgContext->resDataLen = 10;
	}
	else
	{	  
		DescSetNegResponse( (DescNegResCode )LeApplDesc_u_RetCode ); 
	} 
	DescProcessingDone();
}




/*  ********************************************************************************
 * Function name:ApplDescReadDidDID_0xF1F7 (Service request header:$22 $F1 $F7 )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescReadDidDID_0xF1F7(DescMsgContext* pMsgContext)
{
	vuint8 LeApplDesc_u_RetCode;

	LeApplDesc_u_RetCode = DID_ReadDid0xF1F7( pMsgContext->resData );		
	if( kDescNrcNone == LeApplDesc_u_RetCode )
	{
		pMsgContext->resDataLen = 108;
	}
	else
	{	  
		DescSetNegResponse( (DescNegResCode )LeApplDesc_u_RetCode ); 
	} 
	DescProcessingDone();
}


/*  ********************************************************************************
 * Function name:ApplDescReadDidDID_0xF1F8 (Service request header:$22 $F1 $F8 )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescReadDidDID_0xF1F8(DescMsgContext* pMsgContext)
{
	vuint8 LeApplDesc_u_RetCode;

	LeApplDesc_u_RetCode = DID_ReadDid0xF1F8( pMsgContext->resData );		
	if( kDescNrcNone == LeApplDesc_u_RetCode )
	{
		pMsgContext->resDataLen = 12;
	}
	else
	{	  
		DescSetNegResponse( (DescNegResCode )LeApplDesc_u_RetCode ); 
	} 
	DescProcessingDone();
}

/*  ********************************************************************************
 * Function name:ApplDescReadDidDID_0xF1F9 (Service request header:$22 $F1 $F9 )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescReadDidDID_0xF1F9(DescMsgContext* pMsgContext)
{
	vuint8 LeApplDesc_u_RetCode;

	LeApplDesc_u_RetCode = DID_ReadDid0xF1F9( pMsgContext->resData );		
	if( kDescNrcNone == LeApplDesc_u_RetCode )
	{
		pMsgContext->resDataLen = 31;
	}
	else
	{	  
		DescSetNegResponse( (DescNegResCode )LeApplDesc_u_RetCode ); 
	} 
	DescProcessingDone();
}



/*  ********************************************************************************
 * Function name:ApplDescReadDidDID_0xF1FA (Service request header:$22 $F1 $FA )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescReadDidDID_0xF1FA(DescMsgContext* pMsgContext)
{
	vuint8 LeApplDesc_u_RetCode;

	LeApplDesc_u_RetCode = DID_ReadDid0xF1FA( pMsgContext->resData );		
	if( kDescNrcNone == LeApplDesc_u_RetCode )
	{
		pMsgContext->resDataLen = 90;
	}
	else
	{	  
		DescSetNegResponse( (DescNegResCode )LeApplDesc_u_RetCode ); 
	} 
	DescProcessingDone();
}



/*  ********************************************************************************
 * Function name:ApplDescReadDidDID_0xF1FB (Service request header:$22 $F1 $FB )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescReadDidDID_0xF1FB(DescMsgContext* pMsgContext)
{
	vuint8 LeApplDesc_u_RetCode;

	LeApplDesc_u_RetCode = DID_ReadDid0xF1FB( pMsgContext->resData );		
	if( kDescNrcNone == LeApplDesc_u_RetCode )
	{
		pMsgContext->resDataLen = 200;
	}
	else
	{	  
		DescSetNegResponse( (DescNegResCode )LeApplDesc_u_RetCode ); 
	} 
	DescProcessingDone();
}



/*  ********************************************************************************
 * Function name:ApplDescReadDidDID_0xF1FC (Service request header:$22 $F1 $FC )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescReadDidDID_0xF1FC(DescMsgContext* pMsgContext)
{
	vuint8 LeApplDesc_u_RetCode;

	LeApplDesc_u_RetCode = DID_ReadDid0xF1FC( pMsgContext->resData );		
	if( kDescNrcNone == LeApplDesc_u_RetCode )
	{
		pMsgContext->resDataLen = 20;
	}
	else
	{	  
		DescSetNegResponse( (DescNegResCode )LeApplDesc_u_RetCode ); 
	} 
	DescProcessingDone();
}


#if defined (DESC_ENABLE_MULTI_CFG_SUPPORT)
/* ********************************************************************************
 * Function name:ApplDescIsDataIdSupported
 * Description: Additionaly reject a supported PID (multi ECU configuration)
 * Returns:  kDescTrue - if still supported, kDescFalse - if not supported
 * Parameter(s):The PID number
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" may not be called.
 *   - The function "DescSetNegResponse" may not be called.
 ******************************************************************************** */
DescBool ApplDescIsDataIdSupported(vuint16 pid)
{
  return kDescTrue;
}
#endif

/*  ********************************************************************************
 * Function name:ApplDescReadDidDID_0xB005_SEATBACK_PRESS_COUNT (Service request header:$22 $B0 $5 )
 * Description: not available 
 * Returns:  nothing
 * Parameter(s):
 *   - pMsgContext->reqData:
 *       - Points to the first service request data byte.
 *       - Access type: read
 *   - pMsgContext->resData:
 *       - Points to the first writeable byte for the service response data.
 *       - Access type: read/write
 *   - pMsgContext->reqDataLen:
 *       - Contains the count of the service request data bytes (Sid is excluded).
 *       - Access type: read
 *   - pMsgContext->resDataLen:
 *       - Must be initialized with the count of the service response data bytes (Sid is excluded).
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.reqType:
 *       - Shows the addressing type of the request (kDescPhysReq or kDescFuncReq).
 *       - Access type: read
 *   - pMsgContext->msgAddInfo.resOnReq:
 *       - Indicates if there will be response. Allowed only to write only 0 !!!
 *       - Access type: read/write
 *   - pMsgContext->msgAddInfo.suppPosRes:
 *       - UDS only!If set no positive response will be sent on this request.
 *       - Access type: read
 * Particularitie(s) and limitation(s):
 *   - The function "DescProcessingDone" must be called from now on (within this
 * main-handler or later).
 *   - The function "DescSetNegResponse" can be called within this main-handler or later
 * but before calling "DescProcessingDone".
 ********************************************************************************  */
void DESC_API_CALLBACK_TYPE ApplDescRtnCtrlStartRID_0203(DescMsgContext* pMsgContext)
{

	
	if((1 == PendingVehicleSpeed()) &&(1 == PendingPDCM_actualGear()))
	{
		pMsgContext->resData[0] = 0x00;//0xFF;

		pMsgContext->resDataLen = 1;

		DescProcessingDone();

	}
	else
	{	
		DescSetNegResponse(0x22); 
		DescSessionTransitionChecked(DESC_CONTEXT_PARAM_ONLY);
	}
	
	
//  pMsgContext->resData[0] = 0x00;//0xFF;

 // pMsgContext->resDataLen = 1;

 // DescProcessingDone();
}




